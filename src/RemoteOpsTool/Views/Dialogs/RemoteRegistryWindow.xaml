<Window x:Class="RemoteOpsTool.Views.Dialogs.RemoteRegistryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:models="clr-namespace:RemoteOpsTool.Models"
        Title="远程注册表编辑器" Height="600" Width="900"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
       
        Closing="Window_Closing"
        Background="{StaticResource SurfaceDarkBrush}">

    <Window.Resources>
        <Style TargetType="MenuItem">
            <Setter Property="Foreground" Value="{StaticResource OnDarkBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="0"
                                                  ContentSource="Header"
                                                  RecognizesAccessKey="True"
                                                  Margin="{TemplateBinding Padding}"
                                                  VerticalAlignment="Center" />
                                <TextBlock Grid.Column="1" x:Name="GestureText"
                                           Text="{TemplateBinding InputGestureText}"
                                           Foreground="{StaticResource MutedSoftBrush}" FontSize="11"
                                           Margin="24,0,6,0"
                                           VerticalAlignment="Center" />
                                <Path x:Name="SubMenuArrow" Grid.Column="2"
                                      Data="M0,0 L4,4 L0,8 Z" Fill="{StaticResource MutedSoftBrush}"
                                      HorizontalAlignment="Right"
                                      VerticalAlignment="Center"
                                      Margin="4,0,4,0"
                                      Visibility="Collapsed" />
                                <Popup x:Name="PART_Popup"
                                       Grid.ColumnSpan="3"
                                       Placement="Right"
                                       IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                       Focusable="False"
                                       AllowsTransparency="True"
                                       PopupAnimation="Fade">
                                    <Border Background="{StaticResource SurfaceDarkElevatedBrush}" BorderBrush="{StaticResource HairlineBrush}"
                                            BorderThickness="1" CornerRadius="3" Padding="2">
                                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                                            <ItemsPresenter />
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource PrimaryBrush}" />
                                <Setter Property="Foreground" Value="{StaticResource OnPrimaryBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{StaticResource MutedSoftBrush}" />
                            </Trigger>
                            <Trigger Property="HasItems" Value="True">
                                <Setter TargetName="SubMenuArrow" Property="Visibility" Value="Visible" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Separator">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                        <Border Background="{StaticResource HairlineBrush}" Height="1" Margin="8,4,8,4" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ContextMenu">
            <Setter Property="Background" Value="{StaticResource SurfaceDarkElevatedBrush}" />
            <Setter Property="Foreground" Value="{StaticResource OnDarkBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource HairlineBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="2" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <StackPanel IsItemsHost="True" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="8,6,8,4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="路径：" VerticalAlignment="Center" Margin="0,0,6,0" Foreground="{StaticResource MutedBrush}" FontSize="13" />
            <TextBox Grid.Column="1" Text="{Binding CurrentPath, UpdateSourceTrigger=PropertyChanged}" Height="28"
                     Background="{StaticResource SurfaceDarkElevatedBrush}" BorderBrush="{StaticResource SurfaceDarkElevatedBrush}" Foreground="{StaticResource OnDarkBrush}" FontFamily="Consolas" FontSize="13"
                     KeyDown="PathBox_KeyDown" />
            <Button Grid.Column="2" Content="转到" Command="{Binding NavigateCommand}" Width="50" Height="28"
                    Margin="6,0,0,0" Background="{StaticResource PrimaryBrush}" Foreground="{StaticResource OnPrimaryBrush}" BorderBrush="{StaticResource PrimaryBrush}" />
            <TextBlock Grid.Row="1" Grid.ColumnSpan="3" Text="{Binding StatusText}"
                       Foreground="{StaticResource MutedBrush}" FontSize="12" Margin="0,2,0,0"
                       VerticalAlignment="Center" />
        </Grid>

        <Grid Grid.Row="1" Margin="4,0,4,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" />
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TreeView x:Name="RegistryTree"
                      ItemsSource="{Binding RootNodes}"
                      Background="{StaticResource SurfaceDarkSoftBrush}" BorderBrush="{StaticResource SurfaceDarkElevatedBrush}" Foreground="{StaticResource OnDarkBrush}"
                      FontSize="13"
                      VirtualizingStackPanel.IsVirtualizing="True"
                      VirtualizingStackPanel.VirtualizationMode="Recycling"
                      TreeViewItem.Expanded="RegistryTree_Expanded"
                      SelectedItemChanged="RegistryTree_SelectedItemChanged"
                      PreviewMouseRightButtonDown="RegistryTree_PreviewMouseRightButtonDown">

                <TreeView.Resources>
                    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#2F80FF" />
                    <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#2F80FF" />
                    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#FFFFFF" />
                    <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="#FFFFFF" />
                </TreeView.Resources>

                <TreeView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel />
                    </ItemsPanelTemplate>
                </TreeView.ItemsPanel>

                <TreeView.ItemContainerStyle>
                    <Style TargetType="TreeViewItem">
                        <Setter Property="IsExpanded" Value="False" />
                        <Setter Property="ItemsPanel">
                            <Setter.Value>
                                <ItemsPanelTemplate>
                                    <VirtualizingStackPanel />
                                </ItemsPanelTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TreeView.ItemContainerStyle>

                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate DataType="{x:Type models:RegistryTreeNode}"
                                              ItemsSource="{Binding Children}">
                        <TextBlock Text="{Binding Name}" />
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>

            <GridSplitter Grid.Column="1" Width="5" Background="{StaticResource HairlineBrush}" HorizontalAlignment="Stretch" />

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,6">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="{StaticResource SurfaceDarkElevatedBrush}" BorderBrush="{StaticResource HairlineBrush}" BorderThickness="0,0,0,1"
                            Padding="6,4">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" MinWidth="160" />
                                <ColumnDefinition Width="Auto" MinWidth="80" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="名称" Foreground="{StaticResource MutedBrush}" FontSize="12" FontWeight="Bold" />
                            <TextBlock Grid.Column="1" Text="类型" Foreground="{StaticResource MutedBrush}" FontSize="12" FontWeight="Bold" Margin="12,0,0,0" />
                            <TextBlock Grid.Column="2" Text="数据" Foreground="{StaticResource MutedBrush}" FontSize="12" FontWeight="Bold" Margin="12,0,0,0" />
                        </Grid>
                    </Border>

                    <ListBox Grid.Row="1" x:Name="ValueListBox"
                             ItemsSource="{Binding Values}"
                             SelectedItem="{Binding SelectedValue}"
                             Background="{StaticResource SurfaceDarkSoftBrush}" BorderBrush="{StaticResource SurfaceDarkElevatedBrush}" Foreground="{StaticResource OnDarkBrush}"
                             FontFamily="Consolas" FontSize="13"
                             MouseDoubleClick="ValueListBox_MouseDoubleClick"
                             PreviewMouseRightButtonDown="ValueListBox_PreviewMouseRightButtonDown">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="2,3">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" MinWidth="160" />
                                        <ColumnDefinition Width="Auto" MinWidth="80" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding Name}" Foreground="{StaticResource PrimaryBrush}" FontWeight="Bold" />
                                    <TextBlock Grid.Column="1" Text="{Binding Type}" Foreground="{StaticResource MutedSoftBrush}" Margin="12,0,0,0" />
                                    <TextBlock Grid.Column="2" Text="{Binding Value}" Foreground="{StaticResource OnDarkSoftBrush}" Margin="12,0,0,0" TextTrimming="CharacterEllipsis" />
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>

                <WrapPanel Grid.Row="1" Margin="0,4,0,0">
                    <Button x:Name="NewButton" Content="新建 ▾" Width="74" Height="28"
                            Background="{StaticResource PrimaryBrush}" Foreground="{StaticResource OnPrimaryBrush}" BorderBrush="{StaticResource PrimaryBrush}" FontSize="12" Margin="0,0,4,0"
                            Click="NewButton_Click">
                        <Button.ContextMenu>
                            <ContextMenu StaysOpen="False">
                                <MenuItem Header="项(_K)" Click="NewSubKey_MenuClick" />
                                <Separator>
                                    <Separator.Template>
                                        <ControlTemplate TargetType="Separator">
                                            <Border Background="{StaticResource HairlineBrush}" Height="1" Margin="8,4,8,4" />
                                        </ControlTemplate>
                                    </Separator.Template>
                                </Separator>
                                <MenuItem Header="字符串值(_S)" Click="NewStringValue_MenuClick" />
                                <MenuItem Header="二进制值(_B)" Click="NewBinaryValue_MenuClick" />
                                <MenuItem Header="DWORD 值(_D)" Click="NewDwordValue_MenuClick" />
                                <MenuItem Header="QWORD 值(_Q)" Click="NewQwordValue_MenuClick" />
                                <MenuItem Header="多字符串值(_M)" Click="NewMultiStringValue_MenuClick" />
                                <MenuItem Header="可扩充字符串值(_E)" Click="NewExpandStringValue_MenuClick" />
                            </ContextMenu>
                        </Button.ContextMenu>
                    </Button>
                    <Button Content="删除值" Command="{Binding DeleteValueCommand}" Width="70" Height="28"
                            Background="{StaticResource ErrorBrush}" Foreground="{StaticResource OnPrimaryBrush}" BorderBrush="{StaticResource ErrorBrush}" FontSize="12" />
                </WrapPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>



