# RemoteOpsTool 功能测试文档

> 程序版本：v1.4.22
> 文档版本：1.1
> 文档日期：2026-09-22
> 适用产品：RemoteOpsTool - 域环境远程运维工具
> 测试性质：手工功能测试、异常测试、回退路由测试、安全与缓存验证
> 建议执行顺序：先完成环境准备，再按模块从上到下执行；回退专项测试必须放在普通功能测试之后，并在隔离的测试目标机执行。

---

## 1. 文档使用说明

1. 每个用例都要填写“实际结果”和“状态”；不要只勾选“通过”而不记录证据。
2. “前置条件”中未满足的项目，先把用例标记为“阻塞（Blocked）”，不要直接判失败。
3. 建议每个用例至少保存以下一种证据：日志片段、截图、命令输出、目标机注册表/文件/服务前后对比、进程管理器截图。
4. 涉及停止服务、删除打印机、卸载软件、删除注册表、停止进程、注销用户、修改防火墙的用例，只能在虚拟机、测试机或已做快照的机器执行。
5. 文中所有 `TARGET-DOMAIN`、`TARGET-WORKGROUP`、`LOCAL-HOST`、`OPS-ADMIN`、`OPS-LOW` 均为占位符，实际执行时替换为测试环境中的真实主机名、域账号和测试账号。
6. 默认目标机为 `TARGET-DOMAIN`。本机模式的目标为 `LOCAL-HOST` 或当前计算机名。

---

## 2. 测试目标与范围

本测试文档覆盖以下能力：

- 程序启动、版本、主界面、状态栏、日志区。
- 高级设置、PsTools/PsExec 路径、DameWare 路径、域公共目录、调试模式。
- 凭据添加、选择、删除、清空、持久化、错误密码和敏感信息保护。
- Ping、持续 Ping、能力探测、断开/重新连接、DameWare 启动。
- 域公共目录、C$/公共桌面/指定盘符、磁盘信息、磁盘清理。
- 设备管理、服务管理、服务属性、打印机、软件管理、环境变量、系统信息、注册表、计算机管理。
- 远程命令终端：PowerShell、Cmd、Direct、普通执行、交互执行、脚本加载、拖拽、取消。
- 远程交互程序：命令行、PowerShell、计算机管理、打印机管理、注册表编辑器。
- 刷新 DNS、刷新 IP、进程管理、用户会话和注销用户。
- 主机能力缓存、路由学习、PsExec/WMI/DCOM/计划任务回退链。
- 非零退出码不重放、传输失败才回退、权限失败、目标机特殊状态。
- 日志脱敏、凭据文件、路由学习文件和测试环境清理。

不在本文档范围内：

- 负载测试、长时间稳定性测试、域控策略变更、生产环境软件卸载。
- PsExec、DameWare、WMI、RPC、SMB 本身的源码级验证。
- 非 Windows 目标、WinRM 目标、必须安装常驻 Agent 的场景。

---

## 3. 测试环境与前置条件

### 3.1 机器和账号矩阵

| 标识 | 用途 | 要求 | 预期状态 |
|---|---|---|---|
| `LOCAL-HOST` | 操作端/本机模式测试 | Windows 10/11 或 Windows Server，运行 RemoteOpsTool | 可启动 v1.4.22，能打开主窗口 |
| `TARGET-DOMAIN` | 主要域目标机 | Windows 10/11 或 Server，加入测试域，开启 SMB、RPC、WMI、SCM | 有一个活动交互会话；可被 `OPS-ADMIN` 管理 |
| `TARGET-WORKGROUP` | 非域/工作组目标机 | Windows 测试机，使用本地管理员或 SAM 账号 | 用于验证 `主机名\用户`、本地账号和权限差异 |
| `OPS-ADMIN` | 正常运维凭据 | 域管理员或目标机本地管理员，具备远程管理权限 | 能完成大部分读写操作 |
| `OPS-LOW` | 权限不足凭据 | 普通域用户或普通本地用户 | 能登录但部分操作被拒绝 |
| `OPS-BAD` | 错误密码 | 正确用户名、错误密码 | 认证失败，不能执行写入类操作 |
| 无凭据 | 前置校验 | 不选择任何凭据 | 依赖凭据的功能应拒绝执行并给出提示 |

### 3.2 网络和系统组件

在执行正式用例前，确认目标机的以下组件符合测试需要：

- ICMP：允许 Ping，或明确记录 ICMP 被禁用的预期结果。
- TCP 445：SMB、ADMIN$、C$、D$ 等管理共享可用。
- TCP 135 和动态 RPC 端口：WMI/DCOM、服务控制、任务计划 RPC 可用。
- WMI/DCOM：允许 `OPS-ADMIN` 进行 `Win32_*` 查询和 `StdRegProv` 调用。
- SCM：允许创建/启动/停止测试服务以及 PsExec 临时服务。
- ADMIN$：允许 PsExec 写入 `PSEXESVC`。
- 任务计划程序：仅用于交互启动的第三回退通道测试。
- PowerShell：目标机允许执行 PowerShell；记录 ExecutionPolicy、AppLocker/WDAC 是否阻止。
- 目标机已登录一个活动桌面会话，并且该会话可显示 GUI。
- RemoteOpsTool 本机已配置可用的 PsExec 目录，至少包含 `PsExec64.exe` 或 `PsExec.exe`。
- 如果测试 DameWare，需配置可执行的 `DWRCC.exe`；否则只验证“未找到”提示。

### 3.3 测试夹具

建议在 `TARGET-DOMAIN` 上创建以下专用夹具，禁止使用生产对象：

| 类型 | 建议对象 | 说明 |
|---|---|---|
| 服务 | 测试机中可安全启停的非关键服务，例如快照后的 `Spooler`，或自行准备的测试服务 | 不要停止域控、网络、磁盘、远程管理核心服务 |
| 打印机 | `RemoteOps Test Printer` | 仅删除/修改这个打印机 |
| 注册表 | `HKLM\Software\RemoteOpsToolTest` | 所有注册表增删改都在此键下完成 |
| 环境变量 | `REMOTEOPS_TEST_SYSTEM`、`REMOTEOPS_TEST_USER` | 测试完成后删除 |
| 文件和目录 | `C:\Temp\RemoteOpsToolTest\`、`C:\Temp\RemoteOpsToolCleanupTest\` | 只清理此目录，不要测试 `C:\Windows` 等系统目录 |
| 进程 | `notepad.exe`、`timeout.exe` 或专门脚本启动的测试进程 | 结束进程前确认不是系统关键进程 |
| 软件 | 一个可重新安装的测试 MSI/软件包 | 卸载测试不得使用业务软件 |
| 会话 | 一个测试用户会话 | 注销测试用户前确认没有未保存工作 |

### 3.4 安全与恢复要求

1. 测试前对测试目标机做虚拟机快照，或导出服务配置、打印机配置和目标注册表键。
2. 记录目标机测试前后的 `sc.exe query`、`Get-Printer`、`Get-ItemProperty`、`Get-Process` 输出。
3. 防火墙回退测试只能短暂执行，测试后立即恢复原规则。
4. PsExec 测试必须在测试环境中进行，不能在生产域广泛发放临时服务。
5. 任何会改变目标机的命令都不得通过两个通道重复执行；发现重复执行时立即中止并记录 P0 缺陷。
6. 密码、凭据内容和完整 PsExec 命令行属于敏感信息，不能粘贴到缺陷单或公开聊天中。

### 3.5 目标机预检查命令

在 `TARGET-DOMAIN` 的测试管理员 PowerShell 中执行，结果填入测试记录：

```powershell
hostname
whoami
Test-NetConnection TARGET-DOMAIN -Port 445
Test-NetConnection TARGET-DOMAIN -Port 135
Get-Service Winmgmt, LanmanServer, Schedule
query user
Get-Printer | Select-Object Name,DriverName,PortName,Shared
Get-Service | Where-Object { $_.Name -eq 'Spooler' } | Select-Object Status,StartType
Test-Path 'HKLM:\Software\RemoteOpsToolTest'
Test-Path 'C:\Temp\RemoteOpsToolTest'
```

如果目标机当前没有活动会话，交互程序类用例应预期返回“未检测到目标主机活动桌面会话”，而不是启动到无效会话。

---

## 4. 测试结果记录规则

### 4.1 优先级

| 优先级 | 含义 |
|---|---|
| P0 | 凭据、执行通道、非零退出不重放、权限边界、数据安全，失败会阻断主要发布 |
| P1 | 核心功能正常使用，失败会明显影响运维 |
| P2 | 边界、提示、体验、兼容性、低频功能 |

### 4.2 状态

| 状态 | 含义 |
|---|---|
| 通过 | 实际结果与预期一致，证据完整 |
| 失败 | 实际结果与预期不一致，已记录缺陷 ID |
| 阻塞 | 环境、权限、网络或测试夹具不具备，无法得出结论 |
| 未执行 | 尚未执行 |

### 4.3 每条用例至少记录

| 字段 | 说明 |
|---|---|
| 实际结果 | 不要只写“正常”；写明看到的结果、耗时、目标机变化 |
| 使用通道 | 从日志记录实际是 `PsExec`、`WMI/DCOM` 还是 `ScheduledTask` |
| 证据 | 日志时间点、截图文件名、命令输出或文件路径 |
| 缺陷 ID | 失败时填写，例如 `BUG-001` |
| 备注 | 目标机特殊策略、EDR、防火墙、会话状态等 |

---

## 5. 通用验收标准

### 5.1 执行正确性

1. 命令必须在目标主机真实执行，不应只在操作机本地执行。
2. 输出的主机名、用户、文件、注册表、服务结果应与目标机实际状态一致。
3. 命令正常完成且退出码为 0 时，UI 应给出成功或可读结果。
4. 远端命令已启动并返回非零退出码时，应显示命令失败，而不是继续用另一个通道重放。
5. 只有 `TransportResult.IsTransportFailure == true` 时，才应进入下一传输通道。
6. 同一顶层的多步骤操作，例如清理空间、分片上传、删除复核，应复用同一个 session/能力快照。
7. 能使用本机 API 或本机进程完成的操作，不应为了本机目标而走 PsExec 自连接。

### 5.2 凭据与 PsExec

1. 有用户名和密码时，PsExec 必须同时满足：本机 PsExec 使用所选凭据 RunAs 启动，并且 PsExec 命令行显式传入 `-u/-p`。
2. 无凭据时，不应伪造 `-u/-p` 参数。
3. 交互 PsExec 应使用默认服务名 `PSEXESVC`，使用 `-h -n -w -i <session> -d`，不得使用 `-r` 或 `-s` 改变本项目约定。
4. 日志中不得出现明文密码；PsExec 参数中的用户名、密码必须脱敏。
5. 错误密码、权限不足、被 EDR/SCM 拒绝时，应返回可识别错误；不能把命令失败伪装成传输失败。

### 5.3 回退路由

| 操作类型 | 首选顺序 | 主要验证点 |
|---|---|---|
| 普通 Command | WMI/DCOM → PsExec | 日志应先出现 WMI/DCOM；传输失败才走 PsExec；普通短命令不启动 PSEXESVC |
| 显式脚本执行（PreferPsExec） | PsExec → WMI/DCOM | 上传脚本优先 PsExec 以保留流式输出；PsExec 传输失败才回退 WMI |
| 有凭据且命令长度超过约 700 字符 | WMI/DCOM → PsExec | 日志应先出现 WMI；WMI 传输失败才走 PsExec |
| InteractiveLaunch | PsExec → WMI/DCOM → ScheduledTask | 只在前一个通道传输失败时进入下一个 |
| Inventory | WMI/DCOM → PsExec | 优先结构化查询，WMI 不可用时走 PsExec |
| RegistryRead/RegistryWrite | WMI/DCOM → PsExec | WMI 失败后允许 PsExec 回退，但远端非零不重放 |

### 5.4 缓存和性能

1. 重复打开同一功能时，TTL 内应复用缓存，避免每次都完整探测。
2. 手动“刷新”和明确失效动作应绕过或清除缓存。
3. 变更操作后，相关缓存应失效，下一次打开能看到最新状态。
4. 磁盘容量应有单飞控制，多个窗口同时刷新不应产生大量并发查询。
5. 进程、会话、Ping 和命令执行属于实时操作，不应永久缓存。
6. UI 在慢查询期间应保持可操作或有明确的加载状态，不应长时间完全冻结。

### 5.5 日志和错误

1. 日志级别筛选、清空、复制、右键复制/全选应工作。
2. 错误信息应包含目标主机、操作类别和可操作的原因摘要。
3. 传输失败应能区分认证、权限、网络、超时、服务不可用和远端命令失败。
4. 日志重复输出应有合理抑制；同一主机同一路由在短时间内不应持续刷屏。

---

## 6. 测试前准备用例

| 用例ID | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|---|
| PREP-001 | P0 | 测试环境已批准 | 创建 `TARGET-DOMAIN` 快照，记录网络、服务、打印机、注册表、文件和进程基线 | 可以恢复到测试前状态；基线记录完整 |  |  |
| PREP-002 | P0 | `OPS-ADMIN`、`OPS-LOW`、`OPS-BAD` 可用 | 在目标机用每种凭据分别执行 `whoami`、访问 ADMIN$、查询 WMI | 管理员凭据可完成管理操作；低权限和错误密码结果可区分 |  |  |
| PREP-003 | P1 | 目标机在线 | 检查 445、135、WMI、SCM、任务计划程序、活动桌面会话 | 各组件状态已记录，后续失败可判断是环境问题还是程序问题 |  |  |
| PREP-004 | P1 | 目标机可写 `C:\Temp` | 创建测试目录、注册表键、测试打印机和环境变量夹具 | 夹具存在且不影响业务 |  |  |
| PREP-005 | P1 | 本机已安装 PsExec | 打开高级设置，确认 PsTools 状态和 `PsExec64.exe`/`PsExec.exe` 可发现 | 状态显示正常，路径指向真实文件 |  |  |
| PREP-006 | P2 | 有可恢复的测试软件 | 准备测试 MSI 或可重装软件，并记录安装路径和卸载命令 | 后续卸载失败时可恢复 |  |  |

---

## 7. 功能用例

### 7.1 启动、版本与主界面

| 用例ID | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|---|
| APP-001 | P1 | 已发布 v1.4.22 | 双击 `publish\RemoteOpsTool 1.4.22.exe`，观察启动过程 | 主窗口打开，左上角显示 `RemoteOpsTool` 和 `v1.4.22`，无启动错误 |  |  |
| APP-002 | P2 | 主窗口已打开 | 缩小、放大、拖拽窗口到最小尺寸以下，再恢复 | 布局不崩溃，滚动条可用，关键按钮仍可访问 |  |  |
| APP-003 | P1 | 未填写目标主机 | 点击 Ping、能力探测、连接管理等按钮 | 给出“请输入目标主机名”等提示；当前操作不继续 |  |  |
| APP-004 | P1 | 未添加或未选择凭据 | 点击需要凭据的功能按钮 | 给出“请先选择凭据”类提示；不产生无凭据远程写入 |  |  |
| APP-005 | P2 | 主窗口已打开 | 检查状态栏主机名、Ping 状态、连接状态、C/D 盘容量、本地时间 | 字段存在且有初始值或占位值；时间持续刷新 |  |  |
| APP-006 | P2 | 主窗口已打开 | 尝试缩放/切换系统 DPI、切换深色桌面主题 | 文字、按钮、日志区不重叠；可读性和可点击性保持 |  |  |
| APP-007 | P2 | 目标主机名很长或包含短横线、域名后缀 | 输入主机名，观察状态栏和日志显示 | 主机名按规范化名称显示，日志不泄露凭据 |  |  |
| APP-008 | P1 | 已打开多个管理窗口 | 关闭主窗口或关闭子窗口，重新打开子窗口 | 无残留进程、无崩溃、无重复窗口状态异常 |  |  |

### 7.2 高级设置与工具路径

验证入口：主界面底部“高级设置”。

| 用例ID | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|---|
| SET-001 | P1 | PsExec 已安装 | 打开高级设置，浏览/输入有效 PsTools 目录，点击“配置” | 状态显示正常；日志说明已配置；后续命令使用该路径 |  |  |
| SET-002 | P1 | 已知一个不存在的 PsTools 路径 | 输入无效路径，点击“配置”或“保存” | 状态显示未找到；不执行远程写入；提示明确 |  |  |
| SET-003 | P2 | 有可访问的 PsTools 安装包或网络可用 | 依次测试“更新”“卸载”“手动下载” | 每个操作有明确进度或错误提示；失败不破坏原有可用路径 |  |  |
| SET-004 | P1 | 同时存在 PsExec.exe 和 PsExec64.exe | 勾选/取消“优先使用 PsExec64.exe”，保存并执行一条命令 | 保存后重启或重新操作时使用预期版本；日志中的实际程序路径可验证 |  |  |
| SET-005 | P1 | 高级设置已打开 | 将 PsExec 连接超时分别输入 `0`、`3`、`60`、`999` | 输入被限制在 3–60 秒；保存后重新打开值保持合法范围 |  |  |
| SET-006 | P1 | 高级设置已打开 | 设置远端工作目录为 `C:\Windows\Temp`，执行需要工作目录的命令；再设置为不存在路径 | 有效目录下命令可执行；无效目录给出错误，不应静默改成任意目录 |  |  |
| SET-007 | P2 | 高级设置已打开 | 修改临时服务前缀，执行一条 PsExec 命令并查看调试日志 | 日志中的服务名体现前缀；密码仍被脱敏 |  |  |
| SET-008 | P1 | 高级设置已打开 | 打开调试模式，执行操作，关闭调试模式，再重复操作 | 开启时写入 `RemoteOpsTool.log`；关闭后停止新增文件日志；UI 日志仍可用 |  |  |
| SET-009 | P2 | 高级设置已打开 | 修改 DameWare 路径、域公共目录路径，分别用有效和无效路径保存 | 有效路径可被相应功能使用；无效路径有提示或功能被禁用 |  |  |
| SET-010 | P1 | 高级设置已修改但未保存 | 点击“保存设置”，在确认框分别选择取消和确认 | 取消时原设置不变；确认后写入 `%AppData%\RemoteAdmin\settings.json`，重开程序仍保持 |  |  |
| SET-011 | P2 | 已保存配置 | 手工备份后删除 `settings.json`，重启程序 | 程序使用合理默认值启动；不崩溃；重新保存可再次生成文件 |  |  |

### 7.3 凭据管理

验证入口：连接管理区的“凭据管理”“清除凭据”。

| 用例ID | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|---|
| CRED-001 | P0 | 有测试域账号 | 输入域名、用户名、密码、描述，点击“添加” | 凭据出现在列表；显示为脱敏文本；可以单选 |  |  |
| CRED-002 | P0 | 有测试本地账号 | 域名留空或填写目标主机名，分别添加 `用户` 和 `主机名\用户` | 按约定生成完整用户名；本地/工作组账号可用；列表不显示明文密码 |  |  |
| CRED-003 | P1 | 已存在一个账号 | 用同一账号再次添加，大小写不同也尝试一次 | 提示凭据已存在；不产生重复项 |  |  |
| CRED-004 | P1 | 凭据窗口已打开 | 分别测试空用户名、空密码、少于 4 位密码、超过 128 字符用户名、超过 64 字符域名、域名含 `\` 或 `@` | 每种非法输入都被拒绝并给出对应提示；不保存非法凭据 |  |  |
| CRED-005 | P1 | 至少有两个凭据 | 依次选择不同凭据，观察单选状态 | 当前运维凭据只有一个；其他行取消选择 |  |  |
| CRED-006 | P1 | 已有多个凭据 | 选择部分凭据删除，再测试清空全部；重启程序 | 删除项消失，清空后列表为空；重启后状态与保存一致 |  |  |
| CRED-007 | P0 | 已存在 `OPS-BAD` | 选择错误密码凭据，执行 Ping 或能力探测 | 认证失败可见；不使用其他保存凭据冒充；不产生写入 |  |  |
| CRED-008 | P0 | 目标是 `TARGET-DOMAIN` | 使用 `OPS-LOW` 执行能力探测和一个写操作 | 探测可反映权限限制；写操作被拒绝；错误信息说明权限问题 |  |  |
| CRED-009 | P0 | 已保存凭据 | 关闭程序，检查 `credentials.dat` 和日志文件，搜索密码明文 | 文件为 DPAPI 加密数据；日志和配置文件中无明文密码 |  |  |
| CRED-010 | P2 | 密码包含空格、`$`、`!`、`%`、引号等特殊字符 | 添加后执行一条目标机命令 | 密码可被正确传递；命令行不被截断；日志脱敏 |  |  |
| CRED-011 | P2 | 凭据窗口已打开 | 点击密码显示/隐藏按钮，编辑后再切换 | 显示状态切换正常；关闭重开不默认暴露密码 |  |  |
| CRED-012 | P2 | 有大量凭据 | 连续添加、选择和删除，观察窗口 | 列表滚动正常，无重复项，无明显卡顿或异常 |  |  |

### 7.4 连接、Ping、能力探测、DameWare

| 用例ID | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|---|
| CON-001 | P1 | `TARGET-DOMAIN` 在线，ICMP 允许 | 输入主机名，点击“Ping 测试” | 状态栏显示延迟；日志显示目标主机；连接状态更新为成功 |  |  |
| CON-002 | P2 | 已知不可达主机或 ICMP 被禁 | 输入错误主机名或不可达地址，点击 Ping | 显示超时/失败；UI 不崩溃；不继续执行凭据操作 |  |  |
| CON-003 | P1 | 目标机允许 Ping | 点击“持续 Ping”，等待多个周期，再点击“停止 Ping” | 开始后持续更新延迟；停止后不再新增 Ping 日志；按钮文字恢复 |  |  |
| CON-004 | P1 | 已选择 `OPS-ADMIN` | 输入 `TARGET-DOMAIN`，点击“能力探测” | 输出各探测项 OK/FAIL 矩阵；明确 WMI/DCOM、PsExec、计划任务状态 |  |  |
| CON-005 | P1 | 阻断 WMI 或 PsExec 一个通道 | 点击“能力探测”，再执行对应操作 | 探测结果反映不可用通道；执行时只使用仍然可用的通道 |  |  |
| CON-006 | P1 | 已有一次成功连接状态 | 点击“断开连接”，观察状态，再点击“重新连接” | 断开后状态清空或标为断开；重新连接重新 Ping/探测并恢复状态 |  |  |
| CON-007 | P0 | 目标主机填写为本机名或 `localhost` | 在本机模式执行命令、启动 GUI 程序，查看日志 | 本机后台命令走本地进程/API；GUI 走当前桌面 UAC；日志不应出现 PsExec 自连接 |  |  |
| CON-008 | P1 | DameWare 路径有效 | 选择凭据，输入目标主机，点击“远程桌面” | DameWare 进程启动；日志显示连接目标和脱敏用户；不记录密码 |  |  |
| CON-009 | P2 | DameWare 路径无效或未安装 | 清空/填写无效路径，点击“远程桌面” | 提示未找到；不启动空进程；不会调用 PsExec 代替 DameWare |  |  |
| CON-010 | P2 | 目标机名称包含域名后缀 | 分别输入短主机名和 FQDN，执行 Ping/探测 | 两种写法都可正常处理；日志主机名规范化合理 |  |  |
| CON-011 | P1 | 已有能力探测结果 | 5 分钟内再次打开依赖能力快照的功能 | 复用缓存，不重复完整探测；手动“能力探测”可强制刷新 |  |  |
| CON-012 | P1 | 目标机有活动会话 | 点击“能力探测”，观察日志中的交互能力信息 | 能显示活动桌面会话相关结果；无活动会话时不使用无效 Session 1 |  |  |

### 7.5 文件与磁盘

验证入口：文件与磁盘区。这里只测试目标机的网络共享入口和磁盘功能，不要在生产机删除业务文件。

| 用例ID | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|---|
| FD-001 | P1 | 已配置域公共目录路径且路径可访问 | 点击“域公共目录” | 资源管理器打开配置的目录；路径不存在时有提示 |  |  |
| FD-002 | P1 | `OPS-ADMIN` 对目标 ADMIN$ 有访问权限 | 选择凭据，点击“打开 C 盘” | 打开 `\\TARGET-DOMAIN\C$`；能浏览允许访问的目录 |  |  |
| FD-003 | P1 | C$ 可访问 | 点击“公共桌面” | 打开目标机公共桌面目录；不修改文件 |  |  |
| FD-004 | P1 | 目标机存在 D 盘或其他数据盘 | 选择盘符，点击“打开所选盘符” | 打开 `\\TARGET-DOMAIN\D$` 等真实管理共享；盘符显示正确 |  |  |
| FD-005 | P2 | 目标机没有 Z 盘 | 选择 Z:，点击“打开所选盘符” | 给出路径不存在或访问失败提示；不误开本地 Z 盘 |  |  |
| FD-006 | P1 | 使用 `OPS-LOW` 或错误密码 | 点击打开 C$/公共桌面 | 访问被拒绝；错误信息可读；不转换成 PsExec 重试写入 |  |  |
| FD-007 | P1 | 目标机 445/ADMIN$ 可用 | 浏览并在测试目录创建、复制、删除测试文件 | 文件资源管理器操作针对目标机；目标机文件变化可见 |  |  |
| FD-008 | P2 | 有中文、空格、较长路径 | 打开或使用包含中文/空格的测试目录 | 路径正确处理；无乱码、无截断 |  |  |
| FD-009 | P1 | 网络共享可用 | 先把测试文件拖入 C$ 测试目录，再复制回本机并比对哈希 | 上传/下载内容一致；不破坏原文件 |  |  |
| FD-010 | P1 | 目标机在线，已选择凭据 | 点击“获取所有磁盘信息” | 列出盘符、可用空间、总空间、使用率；数字与目标机一致 |  |  |
| FD-011 | P1 | 同一目标机已打开一次磁盘信息 | 立即再次打开，观察日志和耗时 | 20 秒 TTL 内复用缓存；手动刷新可绕过缓存；无重复并发查询 |  |  |
| FD-012 | P2 | 已获取磁盘信息 | 在目标机创建/删除测试目录释放少量空间，观察状态栏刷新 | 主窗口容量按刷新周期更新；磁盘信息窗口可重新刷新 |  |  |
| FD-013 | P1 | 测试机有 `C:\Temp\RemoteOpsToolCleanupTest` | 勾选自定义目录，选择“清空内容”，点击“开始清理” | 目录内容被清理，目录本身按选项保留；完成后有结果提示 |  |  |
| FD-014 | P2 | `C:\Temp\RemoteOpsToolCleanupTest\*.tmp` 存在 | 输入 `C:\Temp\RemoteOpsToolCleanupTest\*.tmp`，勾选“删除目录本身”后再执行 | 只删除匹配文件；删除目录本身的行为符合复选框说明 |  |  |
| FD-015 | P0 | 清理窗口已打开 | 尝试输入 `C:\`、`C:\Windows` 或空路径 | 根目录/关键系统目录保护生效；危险目标被拒绝或要求明确确认 |  |  |
| FD-016 | P1 | 目标路径部分文件被占用 | 执行清理并查看结果 | 可删除项正常删除，占用项保留；显示失败数量与失败路径；不无限循环重试 |  |  |
| FD-017 | P1 | 已完成一次清理 | 查看磁盘容量和清理结果 | 清理后磁盘容量缓存失效并刷新；删除结果经过复核 |  |  |
| FD-018 | P2 | 目标机磁盘为只读或满盘 | 在测试目录尝试复制/创建文件 | 明确提示磁盘只读/空间不足；不产生误导性的成功状态 |  |  |
| FD-019 | P1 | 测试机存在 `C:\Temp\RemoteOpsToolCleanupTest\quoted.tmp` | 在资源管理器中对该文件选择“复制为路径”，将带双引号的原始文本粘贴到自定义清理目标并执行 | 外层双引号自动去除；文件按实际路径清理；日志和结果不出现“目标不存在”误判 |  |  |

### 7.6 设备管理

验证入口：左侧“远程管理”区域的“设备管理”。

| 用例ID | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|---|
| DEV-001 | P1 | 已选择 `OPS-ADMIN`，目标机在线 | 点击“设备管理”，等待列表加载 | 显示设备状态、类别、名称、驱动版本、实例 ID；数据与目标机设备管理器一致 |  |  |
| DEV-002 | P1 | 设备窗口已成功加载过一次 | 关闭后立即重新打开，观察加载时间和日志 | 5 分钟 TTL 内优先显示缓存；不重复完整查询；界面显示缓存时间 |  |  |
| DEV-003 | P1 | 设备窗口已打开 | 点击“刷新” | 强制重新查询目标机；列表更新；缓存时间刷新 |  |  |
| DEV-004 | P2 | 设备列表中包含多个类别 | 在搜索框输入设备名、类别或实例 ID 片段，再清空 | 搜索不区分大小写；只保留匹配项；清空后恢复全部设备 |  |  |
| DEV-005 | P1 | 选择一个可安全禁用的测试设备 | 勾选或选中设备，点击“禁用”，随后刷新 | 仅目标设备被禁用；状态变为禁用；相关缓存已失效；失败时显示明确原因 |  |  |
| DEV-006 | P1 | DEV-005 已成功禁用测试设备 | 选中该设备，点击“启用”，随后刷新 | 设备恢复启用；状态正确；相关缓存已失效 |  |  |
| DEV-007 | P2 | 有可更新的测试设备驱动或测试机快照 | 右键测试设备，点击“更新驱动” | 对目标机设备执行预期操作；出现权限、签名或驱动不匹配时错误可读；不会错误操作本机设备 |  |  |
| DEV-008 | P0 | 使用已快照的测试设备，或明确可卸载的虚拟测试设备 | 选中设备，点击“卸载”，确认后刷新 | 只卸载所选测试设备；有确认提示；卸载失败时不伪造成功；可用快照恢复 |  |  |
| DEV-009 | P1 | 使用 `OPS-LOW` 或无凭据 | 打开设备管理并尝试禁用/启用/卸载 | 只读操作按权限显示；写入操作被拒绝并说明权限不足；不切换到其他通道绕过权限 |  |  |
| DEV-010 | P2 | 未勾选或未选择设备 | 点击“禁用”“启用”“卸载”“更新驱动” | 给出未选择设备提示；不向后端发送空操作 |  |  |
| DEV-011 | P1 | 目标主机填写为本机名 | 本机模式打开设备管理并刷新 | 使用本机系统 API 或本地查询；日志不出现 PsExec 自连接 |  |  |
| DEV-012 | P2 | 设备窗口已打开 | 连续快速点击“刷新”多次 | 不崩溃；避免重复并发全量查询；最终列表与目标机一致 |  |  |

### 7.7 服务管理

验证入口：左侧“远程管理”区域的“服务管理”。

| 用例ID | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|---|
| SVC-001 | P1 | 已选择 `OPS-ADMIN`，目标机在线 | 打开“服务管理”，等待列表加载 | 显示服务名、显示名称、状态、启动类型；数据与 `sc.exe query` 或服务控制台一致 |  |  |
| SVC-002 | P1 | 服务列表成功加载过 | 关闭后立即重开，观察日志和“缓存于”提示 | 15 秒 TTL 内复用缓存；不重复全量查询；显示缓存时间 |  |  |
| SVC-003 | P1 | 服务列表已显示 | 点击“刷新” | 绕过缓存查询目标机；状态和启动类型更新；缓存时间刷新 |  |  |
| SVC-004 | P2 | 服务列表已显示 | 输入服务名或显示名称片段进行搜索，再清空 | 匹配正确、不区分大小写；清空恢复全部 |  |  |
| SVC-005 | P1 | 有一个可安全启停的测试服务，当前为停止 | 勾选/选中服务，点击“启动”，随后刷新 | 目标服务启动；状态变为 Running；操作只执行一次；服务列表缓存失效 |  |  |
| SVC-006 | P1 | 测试服务正在运行，且停止不得影响业务 | 点击“停止”，随后刷新 | 目标服务停止；状态变为 Stopped；缓存失效；失败原因可读 |  |  |
| SVC-007 | P1 | 测试服务可安全重启 | 点击“重启”，观察状态和日志 | 按停止再启动流程完成；完成后状态正确；不会因一次远端非零退出而重复重启 |  |  |
| SVC-008 | P1 | 服务列表已显示 | 右键测试服务，打开“属性”，检查常规和服务登录页 | 显示所选服务的真实配置；切换页签不串到其他服务；关闭窗口后按主机、凭据和服务名保存 2 分钟短缓存，并在再次打开时先展示缓存、后台刷新真实值 |  |  |
| SVC-009 | P2 | 服务属性窗口已打开 | 在启动类型、登录账号、恢复、依赖关系页签做允许的测试性修改，点击“应用/确定” | 修改提交到目标机；重新打开可看到新值；无效值或低权限被明确拒绝 |  |  |
| SVC-010 | P2 | 服务属性窗口已打开 | 验证暂停、恢复按钮的可用性 | 支持暂停/恢复的服务按预期执行；不支持的服务按钮禁用或给出明确提示 |  |  |
| SVC-011 | P1 | 使用 `OPS-LOW` | 尝试启动、停止、重启和修改属性 | 查询按权限显示；写入被拒绝；不通过 PsExec 或 WMI 绕过 SCM 权限 |  |  |
| SVC-012 | P1 | 选择一个系统关键服务但不要实际停止 | 点击“停止”，执行前阅读确认框或错误结果 | 必须有明确确认和风险提示；若环境禁止则拒绝操作；不会无提示停止关键服务 |  |  |
| SVC-013 | P2 | 未选择服务 | 点击“启动”“停止”“重启”“属性” | 给出未选择提示；不产生空命令或误操作 |  |  |
| SVC-014 | P2 | 目标主机填写为本机名 | 本机模式查看、启停测试服务 | 使用本机服务控制 API；日志不出现 PsExec 自连接 |  |  |
| SVC-015 | P1 | 已选择 `OPS-ADMIN`，目标机上存在可安全修改的测试服务 | 打开服务属性 → “登录”页，改为“此账户：”，输入正确的域账户和密码，点击“应用” | 提示已应用；日志显示优先通过 `WMI/DCOM` 下发服务配置（`parameters=[StartName,StartPassword]`）；不出现 `exit=1060`；重新打开属性显示新账户；密码不出现在日志中 |  |  |
| SVC-016 | P1 | 测试服务当前使用指定账户登录 | 打开服务属性 → “登录”页，切回“本地系统账户”，点击“应用” | 应用成功；重新打开显示“本地系统账户”；切换为 LocalSystem 时不强制输入密码 |  |  |
| SVC-017 | P1 | 存在一个“目标机上不存在”的服务名场景（例如服务已被卸载或改名后仍从过期缓存打开） | 对该服务执行启动、停止或“应用”属性 | 明确提示“服务 “xxx” 在目标主机上不存在（错误 1060）…请刷新服务列表”；不再输出难以理解的 `exit=1060` 回退链，也不重复回退多个通道 |  |  |
| SVC-018 | P1 | 测试服务的目标账户密码包含 `"` 或 `%` 字符（可在测试机上构造） | 修改登录账户并输入该密码，点击“应用” | 优先由 WMI/DCOM 成功应用（密码不经命令行）；若 WMI 不可用则明确拒绝命令行回退并给出原因；程序日志与界面均不出现明文密码 |  |  |
| SVC-019 | P2 | 目标机存在“自动(延迟启动)”的服务，或有可安全修改启动类型的测试服务 | 打开服务列表与属性窗口；把启动类型在“自动 / 自动(延迟启动) / 手动 / 禁用”之间切换并应用，再重新打开 | 列表与属性页正确显示“自动(延迟启动)”（不再误判为“自动”或“手动”）；切换后与服务控制台一致；延迟启动经 `sc.exe start= delayed-auto` 落地 |  |  |
| SVC-020 | P2 | 测试服务已使用指定账户登录 | 只修改“恢复”页或启动类型，密码留空，点击“应用” | 允许留空密码（沿用目标主机上已有服务密码）；只下发真正变化的配置项，不覆盖未修改的账户与密码 |  |  |
| SVC-021 | P1 | 已选择 `OPS-ADMIN`，目标机在线，服务列表已加载 | 右键测试服务打开“属性”，观察顶部、基础信息区和日志 | 窗口立即显示服务列表已有的显示名、状态和启动类型；加载期间显示不确定进度条与阶段文字；加载完成前禁止编辑，避免重复提交 |  |  |
| SVC-022 | P1 | 同一目标主机、同一凭据和同一服务在 2 分钟内再次打开属性 | 关闭后立即重新打开；观察显示速度、进度文字和日志 | 立即显示短缓存快照，再后台通过 WMI/DCOM 刷新核心配置；缓存按主机、用户名和服务名隔离；不会把上次未点击“应用”的编辑值当作已保存配置 |  |  |
| SVC-023 | P1 | 服务属性窗口已打开且核心信息加载完成；目标机支持 WMI/DCOM | 首次打开“恢复”页，再切回“常规”后再次打开“恢复”；首次打开“依存关系”页并重复切换 | 首次进入“恢复”时才查询失败恢复策略；首次进入“依存关系”时才查询依赖关系；再次切换同一页签不重复完整远程探测；仅在传输或查询不可用时走 `sc.exe` 回退 |  |  |

### 7.8 打印机管理

验证入口：左侧“远程管理”区域的“打印机”或“打印管理”。

| 用例ID | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|---|
| PRN-001 | P1 | 已选择 `OPS-ADMIN`，目标机 Spooler 正常 | 打开“打印机管理” | 显示名称、驱动、端口、共享、默认状态；与目标机打印机列表一致 |  |  |
| PRN-002 | P1 | 已成功加载打印机列表 | 关闭后立即重开，观察日志和缓存提示 | 1 分钟 TTL 内复用缓存；不重复完整查询；显示缓存时间 |  |  |
| PRN-003 | P1 | 打印机窗口已打开 | 点击“刷新” | 强制重新查询；列表更新；缓存时间刷新 |  |  |
| PRN-004 | P2 | 存在多个打印机 | 输入打印机名、驱动或端口片段搜索，再清空 | 搜索结果正确；清空恢复全部 |  |  |
| PRN-005 | P1 | 存在 `RemoteOps Test Printer` 测试打印机 | 勾选测试打印机，点击“删除”，刷新 | 只删除所选测试打印机；目标机确认已删除；相关缓存失效 |  |  |
| PRN-006 | P1 | 存在两个可安全切换的测试打印机 | 选中一个，点击“设为默认”，刷新 | 目标机默认打印机切换正确；原默认状态更新；缓存失效 |  |  |
| PRN-007 | P1 | 测试打印机存在 | 右键打印机，选择“打印机属性”，检查并关闭属性页 | 管理端本机弹出 Windows 原生属性页，连接的是目标打印机；能看到共享等真实配置；关闭后缓存失效 |  |  |
| PRN-008 | P2 | 测试打印机未共享，且允许改动 | 在属性页修改共享名或共享状态并应用，刷新列表 | 目标机共享状态和列表一致；非法共享名有明确错误 |  |  |
| PRN-009 | P1 | 已选择 `OPS-ADMIN`，目标机 Spooler 正常、RPC 与打印 RPC 可达 | 点击“添加向导”，在管理端本机弹出的原生向导里完成一次网络打印机安装（或直接取消） | 向导窗口出现在管理端本机，不再出现在目标机桌面；日志不出现 `The requested operation requires elevation`；日志显示“已在管理端本机启动添加打印机向导（连接目标打印后台处理程序）”；取消不产生残留更改；成功安装后目标机新增该打印机 |  |  |
| PRN-010 | P1 | 使用 `OPS-LOW` 或错误密码 | 尝试删除、设为默认、修改属性、添加向导 | 操作被拒绝并给出权限/认证提示；不通过其他传输通道重放写入 |  |  |
| PRN-011 | P2 | 未勾选打印机 | 点击“删除”“设为默认” | 给出未选择提示；不执行空删除 |  |  |
| PRN-012 | P1 | Spooler 被停止 | 打开打印机管理或执行查询/写入 | 返回可读的 Spooler/服务不可用提示；不长时间无响应；不使用无关通道伪装成功 |  |  |
| PRN-013 | P2 | 目标主机填写为本机名 | 本机模式打开打印机管理并刷新 | 使用本机打印 API；日志不出现 PsExec 自连接 |  |  |
| PRN-014 | P0 | 已选择 `OPS-ADMIN`，目标机在线，Spooler 正常 | 点击“添加向导”，在管理端本机的向导里选择会忽略 `/c` 的分支（例如“添加本地打印机”），完成安装 | 日志明确告警“检测到打印机被安装到管理端本机而不是目标主机 …请先删除管理端误装的打印机”；不把误装报告为成功；目标机打印机列表不变，管理端本机新增该打印机 |  |  |
| PRN-015 | P2 | 已选择 `OPS-ADMIN`，已通过向导在目标主机成功新增打印机 | 保持打印机管理窗口打开，等待自动校验（退避轮询，最长 5 分钟） | 日志输出“添加打印机向导已在目标主机完成安装: host=… printer=…”；打印机列表自动刷新并出现新打印机；不再重复输出该提示 |  |  |
| PRN-016 | P1 | 已选择 `OPS-ADMIN`，目标机 Spooler 正常；管理机已安装打印管理控制台 | 点击远程管理区域的“打印管理” | 管理机本机打开 Print Management 控制台并自动预置目标打印服务器；可直接看到目标机的打印机；日志包含预置控制台路径；目标机没有新的 GUI 进程或窗口；同一主机重复打开复用稳定控制台文件 |  |  |

### 7.9 软件管理

验证入口：左侧“远程管理”区域的“软件管理”。

| 用例ID | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|---|
| SW-001 | P1 | 已选择 `OPS-ADMIN`，目标机在线 | 打开“软件管理”，等待列表加载 | 显示名称、发行商、版本、注册表键、卸载命令；与目标机已安装程序一致 |  |  |
| SW-002 | P1 | 普通清单已成功加载 | 关闭后立即重开 | 15 分钟 TTL 内复用普通清单缓存；显示缓存时间；不重复完整查询 |  |  |
| SW-003 | P1 | 软件窗口已打开 | 勾选或取消“深度清理”，再刷新 | 普通清单和深度清理清单使用不同缓存键；切换后数据范围与预期一致 |  |  |
| SW-004 | P1 | 软件窗口已打开 | 点击“刷新” | 强制刷新当前模式清单；缓存时间更新；无重复并发查询 |  |  |
| SW-005 | P2 | 列表包含中文、空格或长名称软件 | 搜索名称、发行商、版本，再清空 | 搜索正确、无乱码；清空恢复全部 |  |  |
| SW-006 | P2 | 选中任一条目的卸载命令和注册表键 | 右键分别选择“复制卸载命令”“复制注册表键” | 剪贴板内容与所选条目一致；中文和引号不丢失 |  |  |
| SW-007 | P0 | 使用可重装的测试软件，已有快照 | 选中测试软件，点击“静默卸载”，观察日志和目标机 | 只卸载所选测试软件；返回码和结果明确；成功后软件清单缓存失效；失败不切换到第二个通道重复卸载 |  |  |
| SW-008 | P1 | 测试软件安装包存在，且目标机有活动桌面会话 | 点击“交互式卸载” | 在目标机正确桌面流程中启动卸载程序；只执行一次；无活动会话时给出明确提示 |  |  |
| SW-009 | P0 | 测试软件已卸载，测试快照可恢复 | 勾选“深度清理”，选择测试条目，按界面提示执行清理 | 仅清理与测试软件关联的测试残留；有确认提示；不误删其他软件文件和注册表键 |  |  |
| SW-010 | P0 | 测试项对应 `HKLM\Software\RemoteOpsToolTest` 或其他专用测试键 | 勾选测试条目，点击“清理注册表”，确认执行 | 只删除明确勾选的注册表键；删除后深度/普通清单缓存失效；失败项逐个显示 |  |  |
| SW-011 | P1 | 使用 `OPS-LOW` | 尝试静默卸载、交互式卸载、深度清理、清理注册表 | 写入操作被拒绝；提示权限不足；不发生卸载或注册表删除 |  |  |
| SW-012 | P2 | 未勾选或未选择软件 | 点击卸载、清理注册表和复制操作 | 给出未选择提示；剪贴板不被改写；不发送空命令 |  |  |
| SW-013 | P2 | 测试软件无卸载命令或卸载命令异常 | 执行静默/交互式卸载 | 明确提示缺少卸载命令或执行失败；不清除软件记录；可回退到手工处置 |  |  |
| SW-014 | P1 | 目标主机填写为本机名 | 本机模式加载软件清单 | 使用本机注册表/API 查询；日志不出现 PsExec 自连接 |  |  |

### 7.10 环境变量

验证入口：左侧“远程管理”区域的“环境变量”。目标下拉包含“系统环境变量”以及查询到的活动用户会话环境变量。

| 用例ID | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|---|
| ENV-001 | P1 | 已选择 `OPS-ADMIN`，目标机在线 | 打开环境变量编辑器，默认查看“系统环境变量” | 显示变量名、变量值、作用域；与目标机系统环境变量一致 |  |  |
| ENV-002 | P1 | 目标机存在活动用户会话 | 切换“当前查看”到“会话: 用户名” | 显示该会话用户的环境变量；不同用户/系统作用域不混淆 |  |  |
| ENV-003 | P1 | 系统变量已成功加载 | 切换作用域后再切回，观察加载时间和日志 | 2 分钟 TTL 内复用对应作用域缓存；不重复完整查询 |  |  |
| ENV-004 | P1 | 环境变量窗口已打开 | 点击“刷新” | 强制刷新当前作用域；缓存时间更新 |  |  |
| ENV-005 | P2 | 列表中有多项变量 | 输入变量名或值片段搜索，再清空 | 搜索正确、不区分大小写；清空恢复全部 |  |  |
| ENV-006 | P1 | 系统变量作用域已选择 | 点击“添加”，创建 `REMOTEOPS_TEST_SYSTEM` 和测试值，保存并刷新 | 只新增系统变量；目标机可见；列表刷新；相关缓存失效 |  |  |
| ENV-007 | P1 | 会话用户变量作用域已选择 | 新增 `REMOTEOPS_TEST_USER`，保存并刷新 | 仅修改所选会话用户变量；系统变量不受影响；缓存失效 |  |  |
| ENV-008 | P1 | 测试变量已存在 | 选中变量，点击“修改所选”，修改值并保存 | 变量值只修改一次；目标机值更新；界面刷新；缓存失效 |  |  |
| ENV-009 | P0 | 仅操作测试变量 | 选择测试变量，点击“删除所选”，确认后刷新 | 仅删除测试变量；系统变量和其他用户变量不受影响；缓存失效 |  |  |
| ENV-010 | P2 | 新建变量对话框已打开 | 分别测试空名称、超长名称、包含非法字符、空值 | 按系统规则拒绝非法名称或明确提示；合法空值按实际实现处理；不写入半成品 |  |  |
| ENV-011 | P2 | 测试变量已存在 | 尝试新建同名变量 | 使用覆盖确认或提示重名；不会静默产生重复项 |  |  |
| ENV-012 | P1 | 使用 `OPS-LOW` | 尝试新增、修改、删除系统变量 | 只读查询按权限显示；写入被拒绝；不绕过权限提权 |  |  |
| ENV-013 | P2 | 未勾选变量 | 点击“修改所选”“删除所选” | 按钮禁用或提示未选择；不发送空请求 |  |  |
| ENV-014 | P1 | 目标主机填写为本机名 | 本机模式查看和修改测试环境变量 | 使用本机环境变量 API；日志不出现 PsExec 自连接 |  |  |

### 7.11 系统信息

验证入口：左侧“远程管理”区域的“系统信息”。

| 用例ID | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|---|
| SYS-001 | P1 | 已选择 `OPS-ADMIN`，目标机在线 | 打开“系统信息” | 显示主机名、操作系统及结构化信息；顶部显示实际查询方式 |  |  |
| SYS-002 | P1 | 系统信息已加载 | 展开“原始输出 Raw Output” | 原始输出非空；关键字段与结构化字段一致；长文本可滚动/复制 |  |  |
| SYS-003 | P2 | 系统信息窗口已显示 | 右键选择“复制”“全选”，再用 Ctrl+C、Ctrl+A 验证 | 复制和全选可用；剪贴板内容不乱码、不丢换行 |  |  |
| SYS-004 | P1 | 已正常查询一次 | 关闭并在 5 分钟 TTL 内重开，观察日志 | 复用系统信息缓存；不重复完整探测；显示查询方式或缓存时间 |  |  |
| SYS-005 | P1 | 系统信息窗口已打开 | 等待超过 5 分钟，关闭后重开 | 重新查询目标机；CPU、内存、系统版本等静态信息不显示陈旧值 |  |  |
| SYS-006 | P2 | 目标机网卡/网络地址发生变化 | 通过实时网络功能查看，不依赖系统信息静态缓存 | 动态网络数据使用实时渠道；若系统信息字段为快照，界面必须明确 |  |  |
| SYS-007 | P1 | 使用 `OPS-LOW` | 查询系统信息 | 允许的只读字段正常显示；受限字段给出明确错误或留空；不崩溃 |  |  |
| SYS-008 | P1 | 目标机离线或 WMI 被阻断 | 打开系统信息，等待结果 | 在合理超时内返回 WMI/通道原因；若回退 PsExec 成功则显示结果；不无限等待 |  |  |
| SYS-009 | P2 | 输入短主机名和 FQDN | 分别打开系统信息 | 两种名称均指向同一目标；显示主机名规范化合理 |  |  |
| SYS-010 | P1 | 未选择凭据 | 点击“系统信息” | 不打开无法执行的窗口或给出“请先选择凭据”；不进行匿名远程写入 |  |  |
| SYS-011 | P1 | 目标主机填写为本机名 | 本机模式打开系统信息 | 使用本机 API/WMI 查询；日志不出现 PsExec 自连接 |  |  |

### 7.12 远程注册表

验证入口：左侧“远程管理”区域的“注册表”。所有写操作只在 `HKLM\Software\RemoteOpsToolTest` 或测试用户专用键下进行。

| 用例ID | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|---|
| REG-001 | P1 | 已选择 `OPS-ADMIN`，目标机在线 | 打开远程注册表编辑器，等待初始加载 | 左侧树可展开；右侧显示当前键的值名称、类型、数据；数据与目标机一致 |  |  |
| REG-002 | P1 | 已加载一个注册表路径 | 在 30 秒 TTL 内切换到已访问键再切回，观察日志 | 当前键值复用 30 秒缓存；不重复读取；缓存时间或路径状态可见 |  |  |
| REG-003 | P1 | 注册表窗口已打开 | 点击“刷新” | 绕过缓存重新读取当前键；值列表更新 |  |  |
| REG-004 | P1 | 已知 `HKLM\Software\RemoteOpsToolTest` 存在 | 在“路径转到”输入完整路径并跳转 | 定位到指定键；路径不存在时给出明确提示；不跳转到错误键 |  |  |
| REG-005 | P1 | 已定位到测试键 | 展开子键、选择不同子键、返回上级 | 树、当前路径、值列表同步一致；无越界或重复节点异常 |  |  |
| REG-006 | P1 | 有活动用户会话，测试用户已登录 | 切换到该用户的 HKCU 视图或用户选择入口 | 显示所选用户的 HKCU；不同用户 SID 不混淆；无会话用户时提示不可用 |  |  |
| REG-007 | P1 | 已定位到测试键 | 点击“新建 → 项”，创建 `SubKey`，刷新 | 只创建测试子键；树上可见；目标机注册表确认存在；相关缓存失效 |  |  |
| REG-008 | P1 | 已定位到测试键 | 分别新建字符串、二进制、DWORD、QWORD、多字符串、可扩充字符串值并填写合法数据 | 每种类型均写入成功且类型正确；刷新后持久；非法数据有明确提示 |  |  |
| REG-009 | P1 | 测试值存在 | 双击测试值修改数据并保存，再刷新 | 只有所选值被修改；类型保持；目标机值与界面一致；缓存失效 |  |  |
| REG-010 | P1 | 测试值存在 | 选中测试值，点击“删除值”，确认后刷新 | 只删除所选测试值；其他值不变；删除结果经过读取复核 |  |  |
| REG-011 | P0 | 自行创建的测试子键存在 | 选中测试子键，执行重命名到 `SubKeyRenamed` | 仅测试子键被重命名；目标机可见新名称；原名称不再存在；失败不重复执行 |  |  |
| REG-012 | P0 | 仅 `HKLM\Software\RemoteOpsToolTest` 测试键及其子键存在 | 删除测试子键和测试键，刷新 | 只删除测试对象；无递归误删其他键；有危险操作确认；可据快照恢复 |  |  |
| REG-013 | P1 | 使用 `OPS-LOW` | 尝试新建、修改、删除测试键值 | 读取按权限执行；写入被拒绝；提示 ACL/权限不足；不通过 PsExec 绕过注册表 ACL |  |  |
| REG-014 | P1 | 已定位到只读系统键，例如 `HKLM\SECURITY` 或无写权限键 | 尝试写入测试值 | 操作被拒绝且有可读错误；不生成半成品；不会影响系统键 |  |  |
| REG-015 | P1 | WMI 被阻断但 PsExec 可用 | 打开注册表并读取/写入测试键，检查日志 | RegistryRead/Write 先尝试 WMI/DCOM；仅传输失败时回退 PsExec；远端非零退出不换通道重放 |  |  |
| REG-016 | P2 | 未选择凭据 | 点击“注册表” | 不打开可写窗口或明确提示先选择凭据；不执行匿名访问 |  |  |
| REG-017 | P1 | 目标主机填写为本机名 | 本机模式打开远程注册表工具并操作测试键 | 使用本地注册表 API；日志不出现 PsExec 自连接 |  |  |
| REG-018 | P2 | 注册表窗口已打开 | 输入空路径、超长路径、带非法字符路径 | 给出明确路径错误；不崩溃；不误写其他键 |  |  |

### 7.13 计算机管理

验证入口：左侧“远程管理”区域的“计算机管理”。该功能在操作机桌面打开 MMC，并用所选凭据以 netonly/网络凭据方式连接目标机，不是远程启动目标机 MMC。

| 用例ID | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|---|
| CM-001 | P1 | 已选择 `OPS-ADMIN`，目标机允许远端管理 | 点击“计算机管理”，观察本机 MMC 和目标机连接 | 在操作机打开 MMC；标题/连接目标指向 `TARGET-DOMAIN`；可查看系统工具等允许项 |  |  |
| CM-002 | P1 | 目标机在线，`OPS-ADMIN` 可管理 | 通过 MMC 尝试查看事件查看器、共享文件夹、服务等只读项 | MMC 使用所选凭据连接目标机；显示目标机数据；不把本机数据误认为目标机 |  |  |
| CM-003 | P1 | 使用 `OPS-LOW` | 打开计算机管理并访问受限节点 | MMC 能打开但受限节点报拒绝；不提升为管理员；不切换其他通道绕过 |  |  |
| CM-004 | P1 | 使用 `OPS-BAD` | 打开计算机管理并连接目标机 | 认证失败提示可读；不无限挂起；日志不出现明文密码 |  |  |
| CM-005 | P1 | 未选择凭据 | 点击“计算机管理” | 提示“请先选择凭据”；不匿名建立管理会话 |  |  |
| CM-006 | P2 | 目标机离线或 RPC/SMB 被阻断 | 打开计算机管理并连接 | 在合理时间内显示连接失败原因；本机 MMC 可关闭；RemoteOpsTool 不崩溃 |  |  |
| CM-007 | P2 | 输入短主机名和 FQDN | 分别打开计算机管理 | 都能规范化到正确目标；不会连接错误主机 |  |  |
| CM-008 | P2 | 已打开计算机管理 | 关闭 MMC，再重新打开 | 不残留异常连接状态；RemoteOpsTool 可再次正常启动 MMC |  |  |

### 7.14 发送交互程序

验证入口：左侧“发送交互程序”区域的“命令行”“PowerShell 程序”“计算机管理”“打印机管理”“注册表”。这些按钮用于在目标机活动桌面会话中启动 GUI/交互程序。

| 用例ID | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|---|
| INT-001 | P1 | `OPS-ADMIN` 可用；目标机有一个 Active 桌面会话 | 点击“命令行” | 目标机活动桌面出现 `cmd.exe` 窗口；日志显示实际通道和会话 ID；只启动一次 |  |  |
| INT-002 | P1 | 同上 | 点击“PowerShell 程序” | 目标机活动桌面出现 PowerShell 窗口；窗口身份符合所选凭据；日志无明文密码 |  |  |
| INT-003 | P1 | 同上，目标机具备 MMC 组件 | 点击“计算机管理” | 目标机活动桌面打开 `compmgmt.msc`；不是仅在操作机打开 MMC；只启动一次 |  |  |
| INT-004 | P1 | 同上，目标机具备打印管理组件 | 点击“打印机管理” | 目标机活动桌面打开 `printmanagement.msc`；进程和窗口可验证 |  |  |
| INT-005 | P1 | 同上，目标机允许启动 regedit | 点击“注册表” | 目标机活动桌面打开 `regedit.exe`；不触发 WMI/DCOM 提前退出错误；只启动一次 |  |  |
| INT-006 | P1 | 目标机没有活动桌面会话 | 点击任一发送交互程序按钮 | 明确提示“未检测到活动桌面会话”或等价错误；不使用无效 Session 1；不伪造成功 |  |  |
| INT-007 | P1 | 终端中手动选择一个状态为 Disconnected 或已过期的会话 | 勾选“交互执行”并执行 GUI 启动命令 | 拒绝无效会话或刷新后改选 Active 会话；不把窗口启动到不可见会话 |  |  |
| INT-008 | P1 | 使用 `OPS-LOW` | 点击五个交互程序按钮 | 能启动的操作按权限执行；受策略拦截时显示目标机权限/应用控制错误；不提升到 SYSTEM |  |  |
| INT-009 | P1 | PsExec 被防火墙/EDR/SCM 阻断，WMI 可用，目标机有活动会话 | 点击“注册表”，观察日志中的回退顺序 | 按 `PsExec → WMI/DCOM → ScheduledTask` 顺序；仅 PsExec 传输失败后进入 WMI；最终成功或给出完整原因 |  |  |
| INT-010 | P1 | PsExec、WMI 均被阻断，ScheduledTask 可用 | 点击“命令行”，观察日志 | 前两通道传输失败后进入计划任务；目标机活动桌面启动程序；不会在命令失败后重放 |  |  |
| INT-011 | P0 | PsExec、WMI、ScheduledTask 均被阻断 | 点击“注册表” | 统一失败结果包含各通道原因；不出现部分启动后重复启动；日志可定位被阻断的组件 |  |  |
| INT-012 | P1 | 目标主机填写为本机名，使用 `OPS-ADMIN` | 分别点击“命令行”“PowerShell”“注册表” | 在本机当前桌面通过本地/提权流程启动；不通过 PsExec 自连接；UAC 提示符合系统设置 |  |  |
| INT-013 | P2 | 已启动一个交互命令行窗口 | 再次点击“命令行” | 每次点击对应一次新启动；不会因缓存 capability 快照复用过期的无效会话 |  |  |
| INT-014 | P2 | 本机未安装 PsExec 或路径无效 | 对远程目标点击交互程序 | 若 WMI/计划任务可用则继续回退；否则提示 PsExec 未配置及后续通道结果；不启动错误路径 |  |  |
| INT-015 | P0 | 已打开日志复制功能 | 成功和失败各执行一次交互启动，复制日志并全文搜索密码 | 日志中无明文密码；用户名可脱敏；命令行参数中的凭据不泄露 |  |  |

### 7.15 网络工具、进程管理与用户会话

验证入口：左侧“网络工具”区域的“刷新 DNS”“刷新 IP”“进程管理”。

| 用例ID | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|---|
| NET-001 | P1 | 已选择 `OPS-ADMIN`，目标机在线 | 点击“刷新 DNS”，然后在目标机执行 `ipconfig /displaydns` 或观察操作日志 | 对目标机刷新 DNS 缓存；不是只刷新操作机；结果明确 |  |  |
| NET-002 | P1 | 目标机在线但 DNS 组件/权限受限 | 点击“刷新 DNS” | 清晰显示权限或服务错误；不误报成功；不无限重试 |  |  |
| NET-003 | P1 | 已选择管理员凭据 | 点击“刷新 IP” | 对目标机执行网络配置刷新相关操作；目标机网络连接未意外中断；失败有提示 |  |  |
| NET-004 | P2 | 使用 `OPS-LOW` | 执行“刷新 DNS”“刷新 IP” | 需要管理员的操作被拒绝；只读或允许的操作不受影响；不绕过权限 |  |  |
| NET-005 | P1 | 已选择凭据，目标机在线 | 打开“进程管理” | 显示进程名、PID、会话、内存、CPU 时间、状态、用户名、窗口标题；数据合理 |  |  |
| NET-006 | P2 | 进程列表已显示 | 按名称、PID、用户或窗口标题搜索，再清空 | 过滤正确；清空恢复；输入特殊字符不崩溃 |  |  |
| NET-007 | P2 | 进程列表已显示 | 切换“用户筛选”，选择全部/指定用户 | 只显示该用户进程；计数和实际数据一致 |  |  |
| NET-008 | P2 | 进程列表已显示 | 勾选“实时刷新”，在 2、5、10 秒间切换，再关闭 | 按所选间隔刷新；不会重叠堆积；关闭后停止自动刷新 |  |  |
| NET-009 | P0 | 已在目标机手动启动 `notepad.exe`，记录 PID | 选中该 notepad 进程，点击“结束进程”，刷新 | 只结束指定测试进程；PID 消失；其他进程不受影响；只执行一次 |  |  |
| NET-010 | P0 | 已启动一个带子进程的测试进程树 | 选中父进程，点击“结束进程树”，刷新 | 父进程和子进程按预期结束；非目标进程不受影响；失败有提示 |  |  |
| NET-011 | P2 | 尝试选择系统关键进程，但不实际结束 | 点击“结束进程”或“结束进程树” | 有风险确认或被保护；不会无提示终止关键进程 |  |  |
| NET-012 | P1 | 目标机有测试用户会话 | 在用户会话区域查看用户名、会话 ID、状态 | 会话数据与 `query user` 一致；Active/Disconnected 状态正确 |  |  |
| NET-013 | P0 | 有可注销的测试用户会话且已保存工作 | 选中测试用户，点击“注销所选用户”，重新查询 | 只注销所选测试会话；用户列表更新；不影响其他会话和本机登录 |  |  |
| NET-014 | P1 | 无凭据或使用 `OPS-LOW` | 打开进程管理并尝试结束进程、注销用户 | 查询按权限执行；写入操作被拒绝；不通过其他通道提升权限 |  |  |
| NET-015 | P1 | 目标主机填写为本机名 | 本机模式打开进程管理并结束测试进程 | 使用本机进程 API；日志不出现 PsExec 自连接 |  |  |
| NET-016 | P1 | 目标机离线 | 打开进程管理并刷新 | 在合理超时内提示不可达/传输失败；不永久卡住；不错误显示旧主机数据 |  |  |
| NET-017 | P2 | 进程列表中有无窗口进程和系统进程 | 查看窗口标题、用户名等列 | 空字段合理显示为空白或占位；不出现乱码和错误关联 |  |  |

### 7.16 远程命令终端

验证入口：主界面右侧“远程命令终端输入区”，包括“交互执行”复选框、Shell 下拉框、会话下拉框、“强制结束”“加载脚本”“执行”，以及脚本拖拽区。

| 用例ID | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|---|
| TERM-001 | P1 | 已选择 `OPS-ADMIN`，远程目标在线 | Shell 选择 PowerShell，输入 `whoami; hostname`，点击“执行” | 输出目标机用户名和主机名；退出码为 0；日志显示实际通道；命令不重复 |  |  |
| TERM-002 | P1 | 同上 | Shell 选择 Cmd，执行 `whoami /fqdn` 和 `hostname` | 返回目标机 Cmd 输出；PowerShell/Cmd 参数包裹正确；无乱码 |  |  |
| TERM-003 | P2 | 同上 | Shell 选择 Direct，执行完整可执行命令，例如 `cmd.exe /d /c whoami` | Direct 模式不额外套壳；返回预期结果；引号和路径保留 |  |  |
| TERM-004 | P1 | 目标机有 Active 会话 | 勾选“交互执行”，选择活动会话，执行 `cmd.exe` 或 GUI 入口 | 在活动桌面启动；只启动一次；日志显示会话 ID；不出现无效 Session 1 |  |  |
| TERM-005 | P1 | 目标机无活动会话 | 勾选“交互执行”并执行 | 提示无可用会话；不执行非交互模式冒充成功；不切换到错误会话 |  |  |
| TERM-006 | P1 | 会话下拉中有已断开会话 | 手动选择已断开会话并执行交互命令 | 拒绝无效会话或刷新为活动会话；错误明确 |  |  |
| TERM-007 | P1 | 准备一个只输出 `REMOTEOPS_PS_OK` 的 `.ps1` 文件 | 点击“加载脚本”，选择该 `.ps1`，点击“执行” | 脚本上传/准备到目标机并以 PowerShell 执行；输出正确；只执行一次 |  |  |
| TERM-008 | P1 | 准备一个只输出 `REMOTEOPS_CMD_OK` 的 `.cmd` 文件 | 加载后执行 | 脚本在目标机 Cmd 上下文执行；输出正确；本机不误执行 |  |  |
| TERM-009 | P2 | 准备无副作用测试脚本 | 将脚本直接拖入日志下方“拖拽脚本文件到此可发送到目标主机远程执行”区域 | 识别脚本并执行；显示上传方式和结果；不执行不符合类型或过大的非法文件 |  |  |
| TERM-010 | P1 | 目标机可接收脚本文件 | 在无法使用共享上传的测试条件下加载脚本，观察日志 | 先尝试共享上传；失败后允许通过远程命令分片写入；分片复用同一 session；最终脚本内容哈希一致 |  |  |
| TERM-011 | P1 | 命令长度超过约 700 字符，有凭据 | 执行一条带长参数但结果可验证的命令，观察日志 | 先尝试 WMI/DCOM；仅传输失败后才走 PsExec；结果正确且只执行一次 |  |  |
| TERM-012 | P0 | 已确认命令只执行一次即可观测结果 | 执行会返回非零退出码的命令，例如 `cmd /c exit 7` 或等价命令 | 显示命令失败和退出码；不得因为非零退出而换通道重放；副作用计数为一 |  |  |
| TERM-013 | P1 | 可运行长任务，例如等待 30 秒 | 点击“执行”后点击“强制结束” | UI/命令取消；目标机相关子进程按实现尽力终止；日志说明取消；不崩溃 |  |  |
| TERM-014 | P2 | 终端已打开 | 输入空白、空格和换行后点击“执行” | 不发空命令；给出输入命令为空的提示 |  |  |
| TERM-015 | P1 | 目标主机为离线机，或分别阻断 WMI/DCOM、445/SCM | 执行普通短命令 | 默认先 WMI/DCOM；WMI 传输失败后才回退 PsExec；日志显示实际通道与回退原因 |  |  |
| TERM-016 | P1 | 目标主机为本机名 | 本机模式执行 PowerShell、Cmd、Direct 和脚本 | 使用本地进程执行；不进行 PsExec 自连接；脚本加载路径正确 |  |  |
| TERM-017 | P2 | 目标机 PowerShell 被执行策略/AppLocker 拦截 | 执行 PowerShell 命令 | 显示明确的应用控制/执行策略错误；不伪造成功；其他允许的 Cmd/Direct 入口仍可测 |  |  |
| TERM-018 | P2 | 目标机输出大量文本或包含特殊字符 | 执行产生多行、中文、宽字符和错误流的命令 | 输出完整或明确截断提示；中文不乱码；stdout/stderr 可区分或合并规则一致 |  |  |
| TERM-019 | P1 | 已选择 `OPS-ADMIN`，目标机在线，人为阻断 WMI/DCOM 以确认走 PsExec 回退 | Cmd Shell 执行 `ipconfig /all`，观察日志区开始位置和最终结果 | 不显示 `Connecting to ...`、`Starting PSEXESVC service on ...`、`Copying authentication key ...`、`Connecting with PsExec service ...` 等握手横幅，也不出现连续空行；命令真实输出完整可读 |  |  |
| TERM-020 | P1 | 已选择 `OPS-ADMIN`，目标机在线 | PowerShell Shell 执行 `Get-Item -LiteralPath 'C:\RemoteOpsTool-NotExist-CLIXML' -ErrorAction Stop` | 日志显示可读的“路径不存在”错误，不出现 `#< CLIXML`、`<Objs` 或原始 XML；退出码非零且命令只执行一次 |  |  |
| TERM-021 | P1 | 已选择 `OPS-ADMIN`；阻断 PsExec/445，使 WMI/DCOM 成为实际通道 | 重复 TERM-020，观察日志、通道和退出码 | WMI/DCOM 返回的 PowerShell 错误同样被解码为可读文本；显示实际通道；不因非零退出重放命令 |  |  |
| TERM-022 | P1 | 已选择 `OPS-ADMIN`，目标机在线，PowerShell 模块尚未预热 | PowerShell Shell 执行 `Get-NetAdapter` | 正常时显示网卡结果；缺少网卡模块或权限不足时显示可读错误；不输出 `#< CLIXML`、`Preparing modules for first use.` 或原始 XML |  |  |

### 7.17 日志、能力快照、缓存和路由学习

验证入口：主界面日志区、连接管理“能力探测”、各功能窗口的“刷新”，以及本机 `%AppData%\RemoteAdmin\` 下的持久化文件。测试路由文件前先备份，不要直接删除生产记录。

| 用例ID | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|---|
| OPS-001 | P1 | 已产生不同级别的日志 | 切换日志级别筛选，分别查看 Info、Warn、Error、Debug | 筛选准确；不丢失已生成日志；切回全部后恢复显示 |  |  |
| OPS-002 | P2 | 日志区有多条日志 | 点击“清空日志”，再执行一条命令 | 清空后立即生成的新日志可见；清空不触发远程操作 |  |  |
| OPS-003 | P1 | 日志区有多条日志 | 点击“复制日志”，粘到临时文本；再使用右键复制/全选 | 内容完整、无乱码；敏感信息仍保持脱敏 |  |  |
| OPS-004 | P1 | 同一主机、同一执行方式连续运行多次 | 执行 3–6 次相同或相近操作，观察 10 秒内的日志 | 实际执行日志完整，但“远程执行通道”等非关键重复信息被抑制，不持续刷屏 |  |  |
| OPS-005 | P1 | 目标机和凭据已选定，之前未探测或快照已过期 | 点击“能力探测”，再立即执行一个依赖能力的操作 | 5 分钟内复用同一 host、同一凭据指纹的能力快照；不重复完整探测 |  |  |
| OPS-006 | P1 | 已有未过期的能力快照 | 再次点击“能力探测” | 手动探测强制刷新；日志显示重新探测；结果和可用通道更新 |  |  |
| OPS-007 | P0 | 同一主机存在两组不同凭据 | 分别用 `OPS-ADMIN` 和 `OPS-LOW` 执行能力探测和普通命令 | 能力快照按 host + 用户名 + 凭据指纹隔离；低权限不能复用管理员快照绕过权限 |  |  |
| OPS-008 | P1 | 已成功执行至少一条远程命令 | 关闭程序，确认 `%AppData%\RemoteAdmin\route-learning.json` 存在并重新打开程序 | 路由统计被持久化；重启后仍可读取；文件损坏时不阻止程序启动，而是回退为新学习 |  |  |
| OPS-009 | P0 | `route-learning.json` 已生成 | 查看文件内容并全文搜索测试密码、用户名对应密码、命令文本 | 文件只保存主机、凭据指纹、操作类型、通道、统计和耗时；无明文密码、无命令文本 |  |  |
| OPS-010 | P1 | 人为阻断 WMI/DCOM 但 PsExec 可用 | 执行普通短命令，观察日志和目标机计数 | 先记录 WMI/DCOM 传输失败，再回退 PsExec；命令只执行一次；日志包含回退原因 |  |  |
| OPS-011 | P1 | 人为阻断 PsExec 或 445/SCM 但 WMI/DCOM 可用 | 执行普通短命令 | WMI/DCOM 直接成功，不尝试或等待 PsExec；命令只执行一次；日志显示实际通道 |  |  |
| OPS-012 | P1 | 有凭据，准备超过约 700 字符的可验证命令 | 执行长命令并观察日志 | 首选 WMI/DCOM；只有传输失败才回退 PsExec；长参数未截断；只执行一次 |  |  |
| OPS-013 | P1 | 目标机有活动桌面会话，可分别阻断通道 | 结合 INT-009 至 INT-011 执行交互启动 | 顺序严格为 PsExec → WMI/DCOM → ScheduledTask；前一个通道非传输失败不进入下一个 |  |  |
| OPS-014 | P1 | Inventory 类功能，例如设备、服务或软件清单 | 人为阻断 WMI/DCOM，保留 PsExec | Inventory 先 WMI/DCOM，传输失败后回退 PsExec；同一顶层清单使用同一 session |  |  |
| OPS-015 | P0 | 三通道均阻断 | 执行普通命令和交互启动 | 统一失败信息列出各通道原因；无部分成功；无副作用重复 |  |  |
| OPS-016 | P0 | 目标机可执行一个带可见计数的测试命令 | 执行一个退出码非 0 的命令，例如写入计数后 `exit 7` | 只执行一次；显示非零退出码；严禁切换通道再次执行；计数只增加一次 |  |  |
| OPS-017 | P1 | 磁盘清理、分片脚本上传、删除复核等任一多步骤操作可测 | 在一次顶层操作中观察日志的 session/capability 使用 | 多步骤复用同一能力快照；中途不重新完整探测；不因中途路由变化重复执行副作用 |  |  |
| OPS-018 | P1 | 已打开多个缓存型窗口 | 按各模块变更操作后关闭重开 | 服务、打印机、注册表值、环境变量、设备、系统信息、软件、磁盘按各自 TTL 和失效规则工作 |  |  |
| OPS-019 | P1 | 同时打开磁盘信息主窗口和磁盘信息窗口，或连续快速刷新 | 触发并列刷新，观察目标机查询计数和 UI | 有 single-flight/并发合并；不产生大量重复查询；UI 不长时间冻结 |  |  |
| OPS-020 | P1 | 目标主机为本机名 | 执行本机命令、本机 GUI、本机清单查询 | 本机命令走本地进程/API；本机 GUI 走当前桌面 UAC；日志无 PsExec 自连接 |  |  |
| OPS-021 | P2 | 已备份 `route-learning.json` | 将文件替换为损坏 JSON 后重启程序 | 程序正常启动；警告“读取路由学习缓存失败”并可继续执行；后续记录可重建 |  |  |
| OPS-022 | P2 | 已备份 `route-learning.json` | 检查超过 30 天的旧记录行为，或在隔离环境构造旧记录 | 旧统计不会被永久信任；新探测或真实操作会重新形成记录，不把主机永久钉在某通道 |  |  |
| OPS-023 | P1 | 已选择错误密码 `OPS-BAD` | 执行能力探测和任一批量查询 | 认证失败与传输失败分类正确；不把普通命令失败记为可回退的传输失败；不泄露密码 |  |  |

---

## 8. 回退专项测试

本节建议在隔离测试机使用可控网络/防火墙规则执行。每次只阻断一个通道，记录阻断前配置，测试结束后恢复原配置。

### 8.1 阻断矩阵

| 场景 | PsExec | WMI/DCOM | ScheduledTask | 重点验证 |
|---|---|---|---|---|
| A | 可用 | 可用 | 可用 | 正常首选顺序、缓存、日志不刷屏 |
| B | 阻断 | 可用 | 可用 | 普通命令直接 WMI；不尝试 PsExec；Inventory 仍优先 WMI |
| C | 可用 | 阻断 | 可用 | 普通命令先 WMI，传输失败后回退 PsExec；Inventory 同理 |
| D | 阻断 | 阻断 | 可用 | 交互启动最终进入 ScheduledTask |
| E | 阻断 | 阻断 | 阻断 | 统一失败、无重复副作用 |
| F | 可用 | 可用 | 阻断 | 不得使用计划任务；前两个通道正常工作 |
| G | 可用 | 可用 | 可用 | 远端命令非零退出只执行一次 |
| H | 可用 | 可用 | 可用 | 长命令首选 WMI；失败才回退 PsExec |

### 8.2 回退用例

| 用例ID | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|---|
| FB-001 | P0 | 场景 B | 执行 `hostname` 和写计数测试命令，检查日志与目标机 | WMI/DCOM 直接成功且不尝试 PsExec；计数为 1；实际通道可见 |  |  |
| FB-002 | P0 | 场景 C | 执行普通短命令，检查日志与目标机 | WMI/DCOM 传输失败后 PsExec 成功；不误判命令失败；命令只执行一次 |  |  |
| FB-003 | P0 | 场景 C | 打开设备、服务、软件或注册表读取测试 | Inventory/Registry 先 WMI；如果 WMI 传输失败则回退 PsExec；不重复读多次副作用操作 |  |  |
| FB-004 | P0 | 场景 D，且目标机有活动桌面会话 | 点击“注册表”“命令行”“计算机管理” | PsExec 失败 → WMI 失败 → ScheduledTask 成功；活动桌面只出现一个窗口 |  |  |
| FB-005 | P0 | 场景 E | 执行普通命令和交互启动 | 返回没有可用通道；列出每个通道原因；目标机无命令执行痕迹 |  |  |
| FB-006 | P0 | 场景 F | 执行普通命令和交互启动 | 交互启动不得进入 ScheduledTask；若 PsExec/WMI 可用则正常成功；否则失败清晰 |  |  |
| FB-007 | P0 | 场景 G，准备计数脚本或测试文件 | 执行命令：先记录一次计数，再返回退出码 7 | 结果报告非零退出；计数只增加一次；日志无“命令失败后换通道重试” |  |  |
| FB-008 | P0 | 场景 H，有凭据 | 执行超过约 700 字符的命令，记录目标机副作用次数 | 先 WMI/DCOM；WMI 传输失败才 PsExec；副作用次数为 1；超长参数无截断 |  |  |
| FB-009 | P1 | 目标机 445/ADMIN$ 阻断、WMI 可用 | 执行普通命令、服务列表、软件清单 | 普通命令 WMI 成功；清单 WMI 成功；不因 PsExec 探测失败阻断所有功能 |  |  |
| FB-010 | P1 | WMI 阻断、PsExec 可用 | 读取服务和注册表测试值 | WMI 传输失败后回退 PsExec；返回值正确；非零命令不重放 |  |  |
| FB-011 | P1 | 计划任务 RPC 有效，PsExec/WMI 传输失败 | 从终端交互执行启动 `cmd.exe` | ScheduledTask 只作为第三通道；会话 ID 为真实 Active 会话；成功后不继续尝试其他通道 |  |  |
| FB-012 | P0 | 命令会在目标机产生可见副作用 | 制造远端命令失败（非零退出），同时保持其他通道可用 | 只在原通道失败；不允许“换通道为了再次尝试”造成副作用两次；错误页/日志说明原因 |  |  |
| FB-013 | P1 | 能力快照有效但某个通道临时宕机 | 先能力探测，再在 TTL 内阻断首选通道后执行操作 | 仍然尝试首选通道；收到传输失败后进入回退；不永久信任旧快照；失败统计/冷却生效 |  |  |
| FB-014 | P1 | 路由学习文件中有历史成功记录 | 恢复全部通道后执行同类操作，比较首选顺序 | 学习记录可影响首选但不是无条件覆盖安全顺序；实际失败会降权/冷却；旧记录不会超越权限和操作策略 |  |  |
| FB-015 | P1 | 脚本上传共享路径不可达，但命令通道可用 | 加载脚本并执行，检查目标文件哈希和输出 | 先尝试共享上传；失败后同一 session 使用分片命令写入；不重复上传或执行；最终脚本正确 |  |  |
| FB-016 | P1 | 清理操作目标目录中包含多个文件 | 在 WMI 或 PsExec 传输失败条件下执行清理，观察日志 | 同一顶层操作复用能力快照；失败复核结果清晰；不会因回退重复删除已删除文件 |  |  |
| FB-017 | P1 | 目标机离线后恢复 | 离线时执行一次操作，记录失败；恢复后再次执行 | 失败请求及时结束；恢复后新请求可正常探测和执行；旧失败不会永久阻断可用通道 |  |  |
| FB-018 | P2 | 网络抖动具有短时丢包 | 连续执行只读查询 | 失败退避/冷却避免风暴；恢复后自动成功；UI 不因连续回退而卡死 |  |  |

---

## 9. 测试后清理与恢复

所有清理操作必须在确认测试目标机没有正在进行的测试任务后执行。恢复优先级：防火墙/网络规则 → 服务/打印机/软件 → 注册表/环境变量 → 文件/脚本 → 进程/会话 → 缓存/日志 → 快照确认。

| 用例ID | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|---|
| CLN-001 | P0 | 为回退测试临时修改过防火墙或端口规则 | 按测试前记录恢复 445、135/RPC、WMI、SCM、任务计划相关规则，随后 Ping/探测目标 | 原规则恢复；普通命令、WMI、交互启动按原环境行为执行；没有遗留阻断规则 |  |  |
| CLN-002 | P0 | 曾启停或修改测试服务 | 将测试服务恢复到原状态和启动类型；确认业务关键服务未被修改 | 服务状态与测试前基线一致；没有遗留测试服务 |  |  |
| CLN-003 | P1 | 曾添加、共享、删除或切换打印机 | 删除 `RemoteOps Test Printer` 等测试对象，恢复默认打印机和共享配置 | 打印机列表与基线一致；没有遗留测试打印机 |  |  |
| CLN-004 | P0 | 曾在测试注册表键中操作 | 删除 `HKLM\Software\RemoteOpsToolTest` 及自行创建的用户测试键；对比基线导出 | 测试键全部删除；业务/系统注册表未被修改；无遗留测试值 |  |  |
| CLN-005 | P1 | 曾创建 `REMOTEOPS_TEST_SYSTEM`、`REMOTEOPS_TEST_USER` | 删除测试系统变量、用户/会话变量；刷新环境变量窗口 | 测试变量不存在；其他环境变量保持原值；相关缓存失效 |  |  |
| CLN-006 | P0 | 曾运行清理、脚本上传、文件复制测试 | 删除 `C:\Temp\RemoteOpsToolTest\`、`C:\Temp\RemoteOpsToolCleanupTest\`、上传的测试脚本和临时 base64 文件 | 只删除测试文件；业务文件未被删除；临时目录无残留 |  |  |
| CLN-007 | P0 | 曾启动测试进程和测试用户会话 | 结束测试进程；注销测试用户；确认无遗留子进程 | 目标机进程/会话恢复到基线；无测试脚本残留运行 |  |  |
| CLN-008 | P0 | 曾卸载测试软件 | 使用安装包恢复测试软件或恢复机器快照；检查注册表/服务/文件 | 测试软件恢复到测试前状态；业务软件未受影响 |  |  |
| CLN-009 | P2 | 测试期间产生临时日志、截图、脚本 | 将测试证据保存到受控目录；不要在日志中保留凭据 | 证据可追溯；敏感信息脱敏；临时证据不留在公共目录 |  |  |
| CLN-010 | P1 | 已关闭 RemoteOpsTool | 检查本机是否有残留 RemoteOpsTool、MMC、测试脚本或 PsExec 进程；检查远程临时服务是否已清理 | 无异常残留；程序下次启动正常；目标机无测试用途的 `PSEXESVC` 等残留 |  |  |
| CLN-011 | P0 | 已完成全部清理 | 用基线对比服务、打印机、注册表、环境变量、文件和进程，最后恢复测试快照或签署恢复确认 | 所有测试夹具已移除；业务状态与基线一致；文档中 CLN 用例全部记录结果 |  |  |

### 9.1 清理检查命令

在目标机测试管理员 PowerShell 中执行：

```powershell
Get-Service Spooler | Select-Object Status,StartType
Get-Printer | Select-Object Name,DriverName,PortName,Shared
Test-Path 'HKLM:\Software\RemoteOpsToolTest'
Get-ChildItem -LiteralPath 'C:\Temp' -Force -ErrorAction SilentlyContinue |
    Where-Object { $_.Name -like 'RemoteOpsTool*' } |
    Select-Object FullName,Length,LastWriteTime
Get-Process | Where-Object { $_.ProcessName -match 'RemoteOps|PSEXESVC|notepad|timeout' } |
    Select-Object ProcessName,Id,SessionId,UserName
query user
```

只有确认输出中不存在本次测试创建的对象后，才能把 `CLN-011` 标记为通过。

---

## 10. 测试结果汇总

### 10.1 用例数量汇总

| 模块 | 用例 ID 范围 | 用例数 | 通过 | 失败 | 阻塞 | 未执行 | 备注 |
|---|---|---:|---:|---:|---:|---:|---|
| 测试前准备 | PREP-001 ～ PREP-006 | 6 |  |  |  |  |  |
| 启动、版本与主界面 | APP-001 ～ APP-008 | 8 |  |  |  |  |  |
| 高级设置与工具路径 | SET-001 ～ SET-011 | 11 |  |  |  |  |  |
| 凭据管理 | CRED-001 ～ CRED-012 | 12 |  |  |  |  |  |
| 连接、Ping、能力探测、DameWare | CON-001 ～ CON-012 | 12 |  |  |  |  |  |
| 文件与磁盘 | FD-001 ～ FD-019 | 19 |  |  |  |  |  |
| 设备管理 | DEV-001 ～ DEV-012 | 12 |  |  |  |  |  |
| 服务管理 | SVC-001 ～ SVC-023 | 23 |  |  |  |  |  |
| 打印机管理 | PRN-001 ～ PRN-016 | 16 |  |  |  |  |  |
| 软件管理 | SW-001 ～ SW-014 | 14 |  |  |  |  |  |
| 环境变量 | ENV-001 ～ ENV-014 | 14 |  |  |  |  |  |
| 系统信息 | SYS-001 ～ SYS-011 | 11 |  |  |  |  |  |
| 远程注册表 | REG-001 ～ REG-018 | 18 |  |  |  |  |  |
| 计算机管理 | CM-001 ～ CM-008 | 8 |  |  |  |  |  |
| 发送交互程序 | INT-001 ～ INT-015 | 15 |  |  |  |  |  |
| 网络工具、进程管理、用户会话 | NET-001 ～ NET-017 | 17 |  |  |  |  |  |
| 远程命令终端 | TERM-001 ～ TERM-022 | 22 |  |  |  |  |  |
| 日志、缓存、路由学习 | OPS-001 ～ OPS-023 | 23 |  |  |  |  |  |
| 回退专项 | FB-001 ～ FB-018 | 18 |  |  |  |  |  |
| 清理和恢复 | CLN-001 ～ CLN-011 | 11 |  |  |  |  |  |
| **合计** | **全部用例** | **290** |  |  |  |  |  |

### 10.2 优先级汇总

| 优先级 | 用例数 | 通过 | 失败 | 阻塞 | 未执行 | 发布判断 |
|---|---:|---:|---:|---:|---:|---|
| P0 |  |  |  |  |  | 全部通过，且无未解释的重复执行、权限绕过或数据破坏 |
| P1 |  |  |  |  |  | 核心功能无阻断性失败；失败有明确记录和影响评估 |
| P2 |  |  |  |  |  | 边界和体验问题可接受或有后续计划 |

### 10.3 通道覆盖汇总

| 通道/行为 | 已执行次数 | 成功 | 失败 | 是否发生非传输失败重放 | 证据 |
|---|---:|---:|---:|---|---|
| PsExec |  |  |  |  |  |
| WMI/DCOM |  |  |  |  |  |
| ScheduledTask |  |  |  |  |  |
| 本机进程/API |  |  |  | 不适用 |  |
| 缓存命中 |  |  |  | 不适用 |  |
| 路由学习命中 |  |  |  | 不适用 |  |

### 10.4 发布前结论

```text
测试版本：
测试日期：
测试人员：
被测目标机：
测试凭据类型：
总用例数：
通过数：
失败数：
阻塞数：
未执行数：
P0 失败数：
是否发现重复执行：
是否发现明文密码：
是否发现越权执行：
是否完成环境恢复：
发布建议：通过 / 有条件通过 / 不通过
结论说明：
```

---

## 11. 缺陷记录模板

复制以下模板到缺陷系统；同一问题只建一个缺陷，并在 UI、日志、目标机三处补证据。

```markdown
### BUG-XXX 标题

- 严重级别：P0 / P1 / P2
- 功能模块：
- 关联用例：
- 程序版本：v1.4.22
- 操作机：
- 目标机：
- 凭据类型：管理员 / 普通用户 / 错误密码 / 无凭据（不要写密码）
- 目标机特殊环境：域/工作组、EDR、AppLocker、防火墙、Spooler 状态、活动会话等
- 复现概率：必现 / 高频 / 偶现 / 仅一次
- 首次发现时间：

#### 前置条件

#### 操作步骤

1.
2.
3.

#### 预期结果

#### 实际结果

#### 实际执行通道与回退链

```text
PsExec：
WMI/DCOM：
ScheduledTask：
最终通道：
是否发生回退：
是否发生重复执行：
```

#### 证据

- 日志时间范围和脱敏片段：
- 截图：
- 目标机命令输出/前后对比：
- 路由学习或缓存文件（仅做脱敏截图）：
- 远程进程/服务/注册表状态：

#### 影响范围

#### 规避或恢复措施

#### 修复后回归用例

#### 当前状态

打开 / 已确认 / 修复中 / 待验证 / 已关闭
```

---

## 12. 建议执行顺序

1. **准备阶段**：PREP-001 ～ PREP-006，创建快照、基线、测试夹具和 PsExec 路径。
2. **基础功能阶段**：APP、SET、CRED、CON、FD，只执行低风险查询和测试目录文件操作。
3. **管理功能阶段**：DEV、SVC、PRN、SW、ENV、SYS、REG，先只读后写入，写操作严格限制在夹具上。
4. **交互与实时阶段**：CM、INT、NET、TERM，重点验证活动桌面会话、进程/用户注销和终端取消。
5. **性能与缓存阶段**：OPS-001 ～ OPS-023，记录首轮探测、缓存命中、重复刷新和路由学习前后的耗时。
6. **回退专项阶段**：FB-001 ～ FB-018，每次只阻断一个通道；任何重复执行或越权迹象立即停止测试。
7. **清理恢复阶段**：CLN-001 ～ CLN-011；恢复防火墙、服务、打印机、软件、注册表、环境变量、文件和用户会话。
8. **发布判断阶段**：补齐第 10 章汇总；P0 全通过且无重复执行/明文密码/越权/数据丢失，方可建议发布。

### 12.1 建议记录的性能指标

| 场景 | 首轮耗时 | 缓存命中耗时 | 目标机 CPU/磁盘变化 | 日志通道 | 是否可接受 |
|---|---:|---:|---:|---|---|
| 首次能力探测 |  |  |  |  |  |
| 5 分钟内再次执行普通命令 |  |  |  |  |  |
| 服务列表首轮/15 秒内重开 |  |  |  |  |  |
| 设备列表首轮/5 分钟内重开 |  |  |  |  |  |
| 软件清单首轮/15 分钟内重开 |  |  |  |  |  |
| 磁盘容量首次/20 秒内刷新 |  |  |  |  |  |
| 注册表值首次/30 秒内重开 |  |  |  |  |  |
| WMI 失败后的回退总耗时 |  |  |  |  |  |
| 交互启动成功总耗时 |  |  |  |  |  |

### 12.2 判定提示

- 如果“查询”功能比直接本机/原生查询明显慢，先记录是否命中缓存、是否重复能力探测、是否错误走 PsExec。
- 如果“交互启动”失败，优先确认目标机是否存在 Active 会话，再看 PsExec RunAs 与本机参数传递、WMI 传输和计划任务第三回退。
- 如果“命令返回非零”却再次执行，直接判 P0，不继续做同一目标机上的副作用测试。
- 如果日志持续刷屏，记录重复消息频率、所在主机和操作，判断是否属于日志抑制范围。
- 如果凭据、密码或 `-p` 出现在日志、路由文件或缺陷单中，立即停止分享该文件，按凭据泄露流程处理。
