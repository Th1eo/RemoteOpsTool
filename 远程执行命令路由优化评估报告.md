# RemoteOpsTool 远程执行命令/API 路由与性能优化评估报告

> 审查日期：2026-09-22
> 审查对象：`D:\path\to\RemoteOpsTool`
> 审查基线：`main` 分支，HEAD `577f12a feat: support quoted cleanup paths and release 1.4.22`，标签 `v1.4.22`
> 审查方式：先进行只读代码审查、调用链和回退链核对，随后在 `codex/优化架构` 分支按报告实施优化
> 实施状态：P0-1 至 P0-6、P1-1 至 P1-8、P2-1、P2-2 已完成；已补充回退率、P50/P95、首输出和输出字节指标，以及统一报表与基准测试文档。
> 说明：本文件保留原始评估结论，并在第 13 节记录实施映射。截至本版本尚未执行真实多主机性能基准，文档提供的是可重复执行的测试方案。

---

## 1. 结论摘要

当前程序已经不是“所有功能统一走 PsExec”，也不是“所有功能统一走 WMI/DCOM”，而是混用了四类执行面：

1. **WMI 对象/方法查询**：`Win32_*`、`StdRegProv`、`Win32_Service` 等直接对象查询和 `InvokeMethod`。
2. **WMI raw 命令通道**：通过远程注册表写入命令、`Win32_Process.Create` 启动 PowerShell bootstrap、轮询注册表拿退出码和输出。
3. **PsExec 命令通道**：带凭据本地 RunAs 启动 PsExec，并在 PsExec 参数中显式传入 `-u/-p`，用于流式命令、脚本和交互桌面启动。
4. **本机直连/本机 API**：本机目标不走 PsExec 自连接，而是本地进程、WMI 或显式提权路径。

最重要的判断原则是：

- **结构化只读信息**应优先使用直接 WMI 对象查询，不要启动 PsExec，也不要绕 WMI raw 命令。
- **明确的 WMI 对象方法可以完成的写操作**应优先使用 WMI 方法，例如服务配置 `Win32_Service.Change`、进程终止 `Win32_Process.Terminate`、注册表 `StdRegProv` 写入。
- **需要实时输出、长时间运行、交互桌面或复杂脚本**应优先使用 PsExec；WMI raw 只作为安全回退。
- **短小、无需实时输出的管理命令**可以在 WMI raw 可用时优先 WMI raw，以避免每次启动 `PSEXESVC`；但 WMI raw 有明显固定开销，不能把它当成通用高性能查询 API。
- **所有 PsExec 调用必须遵循现有安全约束**：只要提供运维凭据，就用所选凭据 RunAs 启动本地 PsExec，并同时向 PsExec 传显式 `-u/-p`。

当前最值得优先优化的不是“某一种通道整体替换另一种”，而是以下四类结构性问题：

1. 能力探测一次做全量 Ping、端口、`ADMIN$`、WMI、`query user`、`schtasks`、PsExec `whoami`，导致冷会话和只读操作也被探测链拖慢。
2. 部分查询虽然最终能用 WMI，但先走了远程命令形态，甚至先走 `tasklist`/`netstat` 包装命令，增加了不必要的 WMI raw 或 PsExec 开销。
3. 注册表、服务属性、环境变量等场景存在多次新建 `ManagementScope`、重复查询、逐值调用（N+1）和固定等待。
4. 磁盘状态、能力探测、命令输出和 GUI 状态刷新日志过高频，既增加 UI 负担，也掩盖真正的执行耗时。

---

## 2. 当前路由与回退基线

### 2.1 操作级路由

当前主要策略定义在 `Architecture.md` 和 `src/RemoteOpsTool/Services/Capability/CapabilityMatrix.cs`、`src/RemoteOpsTool/Services/Transports/RemoteExecutionService.cs`：

| 操作类型 | 当前首选顺序 | 设计目的 |
|---|---|---|
| `Command` 普通命令 | `WMI/DCOM → PsExec` | 避免每次短命令启动 `PSEXESVC` |
| `Command` 显式 `PreferPsExec`，且非超长、非交互 | `PsExec → WMI/DCOM` | 脚本、流式输出优先 |
| 超过本地 RunAs 安全长度的有凭据命令 | 固定 `WMI/DCOM → PsExec` | 避免 `CreateProcessWithLogonW` 命令行长限制 |
| `InteractiveLaunch` | `PsExec → WMI/DCOM 一次性任务 → ScheduledTask` | GUI/MMC 需要真实桌面会话 |
| `Inventory` | `WMI/DCOM → PsExec` | 结构化查询优先 |
| `RegistryRead` | `WMI/DCOM → PsExec` | `StdRegProv` 优先 |
| `RegistryWrite` | `WMI/DCOM → PsExec` | WMI 方法优先，命令回退 |

### 2.2 关键安全与一致性规则

- 只有 `TransportResult.IsTransportFailure == true` 才允许进入下一通道。
- 远端进程已经实际启动、返回非零退出码时，属于“命令失败”，不是“通道失败”，不得重新通过另一通道执行。这个规则对删除、服务修改、注册表写入和进程终止尤其重要。
- 本地目标禁止 PsExec 自连接，应使用本地进程/API/UAC。
- PsExec 使用凭据时必须同时满足：
  - 本机以所选凭据 RunAs 启动 `PsExec`；
  - 命令行显式传入 `-u/-p`；
  - 不把密码写入日志；诊断信息只保留掩码身份。
- 路由学习文件只应保存主机、凭据指纹、操作、通道、延迟和失败分类，不应保存密码或命令文本。

### 2.3 能力快照与探测现状

`CapabilityService` 按“规范化主机 + 用户名 + 密码 SHA-256 指纹”缓存快照，默认 TTL 为 5 分钟；同一键并发探测会合并。

`TransportProbeService.ProbeCapabilitiesAsync()` 当前一次同时准备：

- Ping；
- TCP 445、135、5985；
- `ADMIN$` 管理共享；
- WMI/DCOM；
- `query user`；
- `schtasks /Query`；
- PsExec `whoami`。

管理类探测受并发上限 3 控制，PsExec 在 `ADMIN$` 后启动。这个设计对完整能力画像有价值，但对只读 WMI 查询和普通短命令存在明显浪费：

- 查询磁盘、服务、软件、注册表、系统信息，不应先验证 PsExec、`query user`、`schtasks`。
- 交互桌面启动不应该被普通 `whoami` PsExec 探针的结果否决。
- 冷启动时，用户等待的是整条探测链，而不是目标功能本身的耗时。

---

## 3. 三类通道的性能特性

### 3.1 WMI/DCOM 直接对象查询/API

适合：

- `Win32_LogicalDisk` 磁盘容量；
- `Win32_OperatingSystem`、`Win32_ComputerSystem`、`Win32_Processor`、`Win32_BIOS`、`Win32_TimeZone` 等系统信息；
- `Win32_Service` 服务列表、配置、状态和 `StartService`/`StopService`/`Change`；
- `Win32_Process` 进程列表、父子关系和 `Terminate`；
- `Win32_Printer` 打印机列表和打印机方法；
- `Win32_PnPEntity`/`Win32_PnPSignedDriver` 设备信息；
- `StdRegProv` 注册表枚举、读取、写入、删除；
- `ASSOCIATORS OF Win32_DependentService` 服务依赖关系。

优点：

- 结构化返回，不需要解析 `cmd`/PowerShell 文本。
- 不启动 `PSEXESVC`。
- 可以精确选择属性和对象方法。
- 对大多数“查询类”功能延迟最低。

限制：

- 需要 DCOM/RPC 135、WMI 动态端口和目标 WMI 权限。
- 首次建立 DCOM/WMI 连接仍有连接成本，重复创建 `ManagementScope` 会放大成本。
- 某些写操作需要处理 WMI 返回码和部分成功语义。
- 不适合实时输出、长脚本和 GUI 桌面启动。

### 3.2 WMI raw 命令通道

当前 `PsExecService` 中 WMI raw 的实现是：

- 在 `HKLM\SOFTWARE\RemoteOpsTool\WmiJobs\<jobId>` 下写入 Base64 命令和状态；
- 通过 `Win32_Process.Create` 启动编码的 PowerShell bootstrap；
- 子进程把状态、退出码、标准输出和标准错误写回注册表；
- 主程序约每 250 ms 轮询注册表；
- 默认命令超时约 15 分钟；
- 本地 RunAs 安全命令长度阈值为 `MaxSafeRunAsCommandLength = 700`。

适合：

- PsExec 被端点策略阻断时的安全回退；
- 不需要实时输出、执行时间不长的短管理命令；
- 需要避免 `CreateProcessWithLogonW` 命令行长限制的场景。

不适合：

- 高频小查询；
- 实时流式输出；
- 长脚本和交互程序；
- 大量重复调用。每次调用都有注册表写入、进程启动、轮询和清理的固定成本。

### 3.3 PsExec

适合：

- 拖拽脚本、批处理、PowerShell 文件执行；
- 需要实时回传 stdout/stderr 的命令或脚本；
- 需要 `-i <session> -d` 启动桌面 GUI/MMC；
- WMI raw 被 WMI/DCOM 策略阻断时的回退；
- 已经准备好脚本文件并只需要远端 `cmd`/PowerShell 入口的场景。

限制：

- 每次调用通常会启动 `PSEXESVC`，冷启动开销明显；
- `The handle is invalid`、SMB 凭据竞争、服务 RPC 失败会造成通道级失败；
- 命令长度和本地 RunAs 命令行预算有限；
- 输出会混入 PsExec 自身的连接、服务启动、认证 key 和退出信息，需要规范化；
- 凭据通过命令行传给 PsExec 是实现约束，必须严格掩码和审计。

---

## 4. 全功能命令/API 审查与推荐路由

下表的“推荐首选”是运行策略建议，不代表当前已经实现。

| 功能 | 当前实际实现 | 当前主要问题 | 推荐首选 | 推荐回退 | 优先级 |
|---|---|---|---|---|---|
| 系统信息 | 多个 `Win32_*` WMI 查询；失败后 PowerShell/CIM | 大量 `SELECT *`；多次创建 scope；非独立查询并发不清晰 | WMI 对象查询，按需选择属性 | PowerShell/CIM，最后命令通道 | P1 |
| 磁盘空间 | 直接 WMI `Win32_LogicalDisk`；失败走 `Inventory` 命令 | 20 秒缓存、30 秒 UI 刷新、频繁 Debug 日志；每次新建 scope | WMI 对象查询 + 60～120 秒或变更触发缓存 | 远程命令查询 | P1 |
| 磁盘清理 | 批量 PowerShell 脚本；SMB 上传脚本；`Command` 路由；成功后复核 | WMI raw 无实时进度；脚本长度和输出回传受限；清理与进度耦合 | 有实时输出要求时 PsExec 流式；无实时要求时可 WMI raw | 另一通道仅在 transport failure 时 | P1 |
| 设备列表 | `Win32_PnPEntity` + `Win32_PnPSignedDriver`；失败 PowerShell/CIM | 驱动版本匹配存在 O(n*m)；列表查询不应启 PsExec | WMI 对象查询 | PowerShell `Get-PnpDevice`/CIM | P1 |
| 设备启停/卸载 | PowerShell `Enable/Disable/Uninstall-PnpDevice`，按普通命令路由 | 目前是命令式，非直接 WMI 方法；错误分类依赖文本 | 能量化时直接用 PnP/WMI 方法；否则短命令 WMI raw | PsExec 命令回退 | P2 |
| 服务列表 | `Win32_Service`；失败 `Get-Service`；再失败 `sc query state= all` | 回退命令仍可能绕 WMI raw；查询和操作 scope 重复 | 直接 WMI `Win32_Service`，一次性查询 | `sc query`/命令回退 | P0 |
| 服务配置 | WMI 查询优先，`sc qc` 回退；修改优先 `Win32_Service.Change` | 属性窗口多次 `CreateSessionAsync`、多次新建 scope；`sc qc`/`sc query`/`sc qdescription` 多次串行 | 同一 session/scope 的 WMI 对象查询 + 批量字段 | `sc qc` 等仅在 WMI 失败时 | P0 |
| 服务启停/重启 | WMI `StartService`/`StopService`；未生效后 `sc`；重启固定等待约 1500 ms | 固定等待没有利用状态变化；WMI 调用重复查询服务对象 | WMI 方法 + 200～500 ms 状态轮询 | `sc start/stop` | P0 |
| 服务账户/登录修改 | 优先一次 `Win32_Service.Change`，密码作为 WMI 参数，不进入命令行 | 依赖 WMI 返回码映射；域账户、UPN、`.\用户`、特殊字符需专项测试 | WMI `Change` | 仅在明确失败时走受控 `sc config` | P0 |
| 服务失败恢复/延迟启动 | `sc qfailure`、`sc failure` 等 | WMI `Change` 无法表达所有语义，保留 `sc` 是合理的 | `sc` | 无更好替代，避免重复调用 | P1 |
| 打印机列表 | `Win32_Printer`；失败 `Get-CimInstance` | 查询和操作存在重复 scope | WMI 对象查询 | CIM/命令回退 | P1 |
| 打印机添加/删除/共享/默认 | 优先 WMI 方法，失败 `rundll32`/`Set-Printer` | 清默认打印机每次动态 `Add-Type`；GUI 操作依赖交互会话 | WMI 方法；本机用户默认打印机用原生 API | 交互 PsExec/`rundll32` | P1 |
| 软件清单 | `StdRegProv` 优先，失败 `reg query`/PowerShell | 多键枚举和值读取可能是多次 WMI 往返；15 分钟缓存合理 | WMI `StdRegProv` 批量枚举 | `reg.exe`/PowerShell | P0 |
| 软件卸载 | 读取注册表中的 Uninstall 命令，按 `Command` 路由 | 卸载命令形态不可控，输出和退出码语义容易混淆 | 已知卸载程序：PsExec 流式；短命令可 WMI raw | 只在 transport failure 回退 | P1 |
| 软件深度清理 | `StdRegProv` 删除优先，失败 `reg delete /reg:64` | 需要验证键是否存在、64 位视图和部分失败 | WMI `StdRegProv`，批量删除 | `reg delete` 命令 | P1 |
| 环境变量读取 | `StdRegProv`；先 `EnumValues`，再逐值 `GetStringValue` | 典型 N+1 WMI 往返；HKCU/SID 解析也有额外查询 | 复用 scope + 批量/并行读取 | 注册表命令回退 | P0 |
| 环境变量写入 | `StdRegProv.SetStringValue`/删除 | 每次可能新建 scope | 复用 WMI 连接和类对象 | 注册表命令回退 | P1 |
| 注册表子树 | `StdRegProv.EnumKey` | 每个节点一次 WMI 调用，适合按需展开 | WMI `StdRegProv`，按需加载 | `reg query` | P1 |
| 注册表值 | `EnumValues` 后对每个值调用一次 `ReadRegistryValue` | N+1 往返是最明显的注册表性能问题 | 批量读取/远程注册表原生 helper | `reg query`/PsExec | P0 |
| 注册表写入/删除 | `StdRegProv` 优先，命令回退 | 多个修改操作各自创建 session；部分失败需明确原子性 | 复用 session/scope | 受控 `reg.exe` | P0 |
| 进程列表 | 远程先调用 `tasklist /v` 方法，再直接 `Win32_Process`；但 `tasklist` 请求经 `Inventory` 路由，实际通常先走 WMI raw | 方法名和实际路由容易误判；已能用结构化 WMI 却先执行文本命令 | 直接 WMI `Win32_Process`，一次查询 | `tasklist`/PsExec | **P0** |
| 进程终止 | WMI `Win32_Process.Terminate`；失败 `taskkill`/PsExec | 进程树先全量查询再逐个 Terminate；可复用进程快照 | WMI 对象方法 | `taskkill`/PsExec | P1 |
| 活动连接/端口 | 一条 PowerShell 包装 `tasklist` + `netstat -ano`，按 `Inventory` 路由 | 混合文本解析；进程名和连接表两次数据源；包装命令增加启动成本 | 原生 `netstat` 或 IP Helper helper；不要先转 WMI | PsExec 命令回退 | P1 |
| 用户会话 `query user` | 优先 WMI explorer owner，失败 `query user`；注册表也有 SID 枚举 | 会话探测经常作为全量能力的一部分；远程 `query user` 可能慢 | `query user` 短缓存（5～15 秒）+ 按需探测；更可用的场景使用 WMI owner | PsExec | P0 |
| 注销用户 | `logoff <sessionId>`，按命令路由 | 需要确认会话 ID 和权限；命令失败不能换通道重放 | PsExec/短命令 WMI raw，取决于输出需求 | 另一通道仅 transport failure | P2 |
| DNS 刷新、IP 释放/续租 | `ipconfig.exe /flushdns`、`/release`、`/renew` | 两条命令分别创建/执行；没有合并或状态反馈 | 合并为一次可控命令；PsExec 或 WMI raw 按需 | 另一通道仅 transport failure | P2 |
| 终端普通命令 | 当前代码默认 `WMI/DCOM → PsExec` | WMI raw 对短命令合理，但实时输出差；PsExec 对交互命令流式体验好 | 短、无实时要求：WMI raw；需要实时输出：PsExec | 另一通道 | P1 |
| 拖拽脚本 | SMB 上传脚本文件后显式 `PreferPsExec`，`PsExec → WMI` | 关键应保证脚本文件路径、实时输出、退出码和进度状态一致 | PsExec 流式执行脚本文件 | WMI raw 回退 | P0 |
| MMC/GUI/交互程序 | `PsExec -i <session> -d` 优先；WMI 一次性任务和计划任务回退 | 会话探测、`The handle is invalid`、SMB/RPC 失败会触发回退 | PsExec 真实交互桌面 | WMI 一次性任务/ScheduledTask | P0 |
| DameWare | 本机启动 `DWRCC.exe`，不是远程命令通道 | 只影响本机 GUI 启动，不需要纳入 WMI/PsExec 路由 | 本机进程启动 | 无 | - |

---

## 5. 推荐路由决策矩阵

### 5.1 按功能类型

| 功能类型 | 推荐首选 | 推荐回退 | 说明 |
|---|---|---|---|
| 结构化只读查询 | 直接 WMI 对象查询 | 原生命令/PsExec | 磁盘、进程、服务、设备、软件、注册表、系统信息 |
| WMI 可直接完成的写操作 | WMI 对象方法 | 受控命令回退 | 服务 `Change`、进程 `Terminate`、注册表 `StdRegProv` |
| 短小管理命令 | WMI raw | PsExec | 无需实时输出时优先减少 `PSEXESVC` 启停 |
| 长脚本/拖拽脚本 | PsExec + 文件化执行 | WMI raw | 先把脚本上传为文件，再执行文件，不要拼接超长命令行 |
| 需要实时 stdout/stderr | PsExec 流式 | WMI raw 仅用于最终结果 | WMI raw 是轮询注册表，不是流式通道 |
| GUI/MMC/桌面程序 | PsExec `-i <session> -d` | WMI 一次性任务/ScheduledTask | 必须匹配真实登录会话 |
| 注册表批量读 | 原生远程注册表/WMI 批量 helper | `StdRegProv` 逐值、`reg.exe` | 当前逐值 WMI 是主要瓶颈 |
| 端口/连接表 | 原生 `netstat` 或 IP Helper helper | PsExec 命令回退 | 不应无实测地改成 WMI |
| 会话查询 | `query user` 短缓存 | PsExec | 只在使用会话相关功能时探测，不要全量探测 |
| Ping/连通性 | 本地 `Ping`/TCP 探测 | WMI/PsExec 能力探测 | 不要用 PsExec 判断主机是否可达 |
| 本机操作 | 本地 API/本地进程/UAC | 无远程通道 | 禁止 PsExec 自连接 |

### 5.2 按命令形态

| 命令形态 | 推荐路由 |
|---|---|
| `Win32_*` 能直接表达 | WMI 对象查询，不要先执行 PowerShell |
| 短命令，最终返回文本即可 | WMI raw 优先，PsExec 回退 |
| 命令运行时需要逐步显示输出 | PsExec 流式优先 |
| 命令长度接近/超过 700 字符 | 先上传为脚本文件；否则 WMI raw；不要依赖 PsExec 命令行拼接 |
| 命令包含引号、`%`、`!`、中文路径 | 优先对象/API；必须命令时减少 shell 层级并做好转义测试 |
| 交互 GUI、MMC、控制面板 | PsExec `-i`，不要使用重定向输出路径 |
| 破坏性写操作 | 先行能力判断，命令真实启动后的非零退出不得重放 |

---

## 6. 重点优化项

### P0-1：能力探测按 operation/profile 分层

**问题**

`ProbeCapabilitiesAsync()` 每个冷快照都执行完整探测链。对只读系统信息、磁盘、注册表、服务列表等操作，PsExec、`query user`、`schtasks` 探测并没有必要。

**建议**

引入 operation/profile，而不是只有一个全量 `ProbeCapabilitiesAsync`：

- `InventoryWmiOnly`：Ping/TCP（可选）+ WMI/DCOM，不探测 PsExec/query/schtasks。
- `RegistryWmi`：只探测 WMI/DCOM，必要时再探测命令回退。
- `Command`：WMI/DCOM + PsExec 按需探测。
- `InteractiveLaunch`：PsExec 交互探针 + WMI 一次性任务 + 计划任务。
- `SessionQuery`：只探测 `query user` 或对应 WMI owner 查询。

同时增加“按需升级”：首次只用 WMI 查询，如果真实操作返回 transport failure，再触发 PsExec/计划任务探测。这样不会为了一个查询提前付出 PsExec 启动成本。

**收益**

冷启动和只读功能预计最明显，尤其能消除“打开一个查询窗口就等 PsExec whoami”的体感。

### P0-2：进程列表改为直接 WMI `Win32_Process` 优先

**问题**

`NetworkService.GetProcessListAsync()` 远程分支先调用 `TryGetProcessListViaPsExecTaskListAsync()`，失败后才调用 `TryGetProcessListViaWmiAsync()`。虽然 `tasklist` 请求通过 `Inventory` 路由器可能先走 WMI raw，但代码意图和架构表已经出现漂移：

- 文本命令需要经过 shell、WMI raw/PsExec、输出解析；
- `tasklist /v` 输出比 `Win32_Process` 属性大；
- 失败时才做结构化 WMI，用户等待时间被放大。

**建议**

- 远程进程列表直接 `SELECT Name,ProcessId,SessionId,WorkingSetSize FROM Win32_Process`，一次查询。
- `tasklist` 仅作为 WMI 不可用时的命令回退。
- 本机仍可使用更快的本地 `tasklist` 或本地 WMI，由实测决定。
- 进程树终止可以复用刚才的进程快照，不要再次全量查询。

**收益**

减少一次不必要的命令启动或 WMI raw 作业，降低输出解析和失败等待。

### P0-3：活动连接/端口不要先包装成 PowerShell 混合命令

**问题**

`GetActiveConnectionsAsync()` 目前把 `tasklist` 和 `netstat -ano` 包装成一条 PowerShell 命令。该命令经 `Inventory` 路由时可能先走 WMI raw，然后失败再走 PsExec。它同时调用两个子进程、生成两种文本输出，并依赖分隔符解析。

**建议**

- 先以原生 `netstat -ano` 作为基准实现，进程名映射可单独用 WMI `Win32_Process` 批量获取。
- 若必须做高性能连接表，评估远程 helper 调用 IP Helper API（`GetExtendedTcpTable`/`GetExtendedUdpTable`）。
- 不要在没有基线数据的情况下直接把 `netstat` 替换成 WMI；WMI 没有等价的完整连接表对象。
- 连接列表属于实时数据，不应长期缓存，但可以做 2～5 秒 UI 防抖。

### P0-4：注册表值读取消除 N+1 WMI 调用

**问题**

`RemoteRegistryViewModel.TryLoadRemoteValuesViaWmiAsync()` 先调用 `EnumValues`，然后对每个值调用一次 `ReadRegistryValue()`，每次都是一次远程 WMI `InvokeMethod`。`EnvVarService` 的注册表变量读取也有相同形态。

**建议**

- 短期：复用 `ManagementScope` 和 `ManagementClass`，限制并发并提供批量读取适配器。
- 中期：实现一个远程注册表 helper，使用 `RemoteRegistry`/Win32 注册表 API 一次枚举和读取多个值；只在 helper 不可用时回退 `StdRegProv`。
- 长期：对软件清单、环境变量、注册表树的重复键做明确的 snapshot cache；修改后按路径/前缀精准失效。

**收益**

值较多的注册表键，减少往返次数通常比换通道收益更大。

### P0-5：服务属性窗口复用 session 和 WMI scope

**问题**

`ServicePropertiesViewModel` 多处调用 `CreateSessionAsync()`，WMI 查询又多次创建 scope。打开属性窗口时会串行执行多个查询：

- 服务配置；
- 服务状态；
- 依赖服务；
- 失败恢复策略；
- 服务描述；
- 账户/登录信息。

其中部分数据和 WMI 服务对象可以一次拿到。

**建议**

- 属性窗口生命周期内复用一个 `IRemoteExecutionSession`。
- 复用一个 `ManagementScope` 和一次 `Win32_Service` 查询，尽量一次拿齐公共属性。
- `sc qc`、`sc query`、`sc qdescription` 只在对应 WMI 字段缺失或 WMI 整体失败时执行。
- 账户修改继续优先 `Win32_Service.Change`；密码作为 WMI 参数传递，不进入命令行。
- 服务状态变化改为轮询，而不是固定等待 1500 ms。

### P0-6：能力缓存与路由学习按 transport 而不是按整体探测

**建议**

- 保留主机 + 用户名 + 凭据指纹缓存，但将 TTL 区分为：
  - Ping/TCP：较短；
  - WMI：中等；
  - PsExec 可用性：短 TTL 或失败冷却；
  - `query user`：只在使用会话功能时缓存 5～15 秒。
- 负缓存只影响对应通道的冷却，不要因为 PsExec 整体不可用就阻断 WMI 查询。
- 路由学习增加操作、命令形态、结果规模和耗时分位，避免只记录“某个通道曾经成功”。

### P1-1：WMI 连接复用和受控并发

当前多个服务、属性页和直接查询会新建 `ManagementScope`。建议引入按“主机 + 凭据指纹 + WMI namespace”复用的连接对象或轻量连接池：

- 同一服务列表、磁盘、系统信息、服务属性只在 session 内创建一次；
- 独立查询可在 2～4 个并发内执行，不能无限并发冲击 DCOM；
- 连接失败后做短冷却，避免反复建立失败连接；
- 退出/切换目标主机时释放。

注意 `ManagementScope` 的线程安全和 DCOM 生命周期需要实测，不要直接在多个线程共享同一个未受控 scope。

### P1-2：系统信息和磁盘查询做选择性属性查询

`SystemInfoService` 当前多类查询中使用了较大的对象集合。建议：

- 明确每个 UI 字段需要哪些属性，避免 `SELECT *`；
- 磁盘只读取 `DeviceID/FreeSpace/Size`；
- 系统信息和 QFE/视频控制器等相互独立的查询做受控并行；
- 保持 5 分钟系统信息缓存，但刷新按钮应能强制刷新并显示缓存年龄。

### P1-3：磁盘状态 UI 刷新降频并减少日志

已知磁盘信息有约 20 秒静态缓存，主窗口约每 30 秒刷新，且每次刷新都输出 Debug 日志。建议：

- UI 刷新周期改为 60～120 秒，或在切换主机、打开相关窗口、手动刷新时触发；
- 只有在容量变化超过阈值、查询失败或用户强制刷新时输出 Info；
- Debug 模式下采样输出，避免每次定时刷新都打印相同内容。

### P1-4：服务状态操作改轮询

当前重启流程是 WMI stop → 固定等待约 1500 ms → WMI start。建议：

- 每 200～500 ms 查询一次服务 `State`；
- 直到 `Stopped` 或超时，再执行 start；
- 对 `StartPending`/`StopPending` 给出 UI 状态；
- 超时后明确区分“命令失败”和“状态未达到目标，未重放”。

### P1-5：清默认打印机移除动态 `Add-Type`

清默认打印机的回退路径每次动态编译 C# P/Invoke。建议：

- 改为预编译的托管 helper 或固定原生模块；
- 或者使用已存在的打印机 API/CLI，但必须实测用户会话默认打印机语义；
- 本机操作不要走远程命令通道。

### P1-6：设备驱动版本匹配从 O(n*m) 改为索引

设备列表同时获取 PnP 设备和驱动版本时，当前匹配方式可能造成设备数 × 驱动数的扫描。建议建立 `InstanceId` 索引或字典/trie，再进行一次线性匹配。

### P1-7：软件清单批量枚举

软件清单已经有 15 分钟缓存，这是合理的。重点优化：

- 64 位/32 位 Uninstall 键批量枚举；
- 每个键一次性读取需要的属性；
- 深度清理按注册表前缀和目标键精确失效；
- 避免每次打开软件管理都重新执行完整远程脚本。

### P1-8：日志和输出通道分离

当前 PsExec 协议行、WMI 回退行和脚本输出都进入日志区，空行也会记录，造成噪声。建议：

- PsExec 连接、服务启动、认证 key 折叠为 Debug/协议级；
- 远端 stdout/stderr 进入“命令输出”区；
- 错误摘要只保留一次，失败尝试可以汇总，不要重复堆叠；
- 对相同文本做短窗口去重；
- 日志中不得出现明文密码，PsExec 命令行只记录掩码结果。

### P2-1：路由学习增加延迟指标

建议记录：

- `Operation + Transport + CommandShape`；
- P50/P95 延迟；
- 成功率、transport failure 率、命令非零率；
- 输出大小和首次输出时间；
- EWMA 或滑动窗口，避免一次慢请求就永久改变路由。

### P2-2：建立统一的基准测试

至少测试四类目标：

- 冷启动/热会话；
- WMI 可用、PsExec 可用、两者同时可用、仅一条可用；
- 小命令、长命令、长脚本、交互 GUI；
- 服务、注册表、软件、磁盘、进程、端口、会话。

每个用例记录首字节时间、完成时间、通道、失败码和是否发生回退。

---

## 7. PSEXEC 命令长度处理评估

当前代码中的关键常量是：

- `PsExecService.MaxSafeRunAsCommandLength = 700`；
- 有凭据、非交互、命令超过该长度时，`RemoteExecutionService` 固定使用 `WMI/DCOM → PsExec`；
- 拖拽脚本通过 SMB 上传为文件，再执行文件路径，因此不依赖超长命令行；
- WMI raw 本身通过注册表存放命令，bootstrap 的命令行长度不随完整命令线性增长。

需要明确的边界：

1. **本地 RunAs PsExec 启动**有命令行长限制，所以 700 字符是一个保守的安全阈值，不是“Windows PsExec 只能执行 700 字符”。
2. **WMI raw 解决的是本地 RunAs 命令行预算问题，不是无限命令长度问题**。远端 PowerShell/cmd 本身仍有参数和脚本解析限制。
3. 对超过阈值的终端命令，当前不会自动拆分或压缩，而是转 WMI raw；长命令的稳定做法应是“上传脚本文件 + 执行文件”，而不是继续拼接参数。
4. 对脚本、包含大量引号或 `%` 的命令，优先文件化执行，避免多层 `cmd /c`、PowerShell wrapper 和 WMI registry bootstrap 的转义叠加。
5. 不要为了绕过长度限制而牺牲“命令非零不得重放”的语义。文件已经启动执行后返回非零，只能报告失败，不能换通道再执行一次。

---

## 8. 风险、边界与不应优化的场景

### 8.1 不应把 WMI raw 当通用快速通道

WMI raw 每次都要：

- 写注册表 job；
- 启动 PowerShell bootstrap；
- 轮询状态；
- 读取注册表输出；
- 清理 job。

因此它适合“短命令的安全回退”和“避免 PSEXESVC 启动”的受控场景，不适合高频查询。

### 8.2 不应为纯查询启动 PsExec

系统信息、磁盘、服务列表、软件清单、打印机、设备、注册表等纯查询，优先使用 WMI 对象或本机 API。只有 WMI transport failure 时才进入 PsExec。

### 8.3 不应无实测把 `netstat` 改成 WMI

连接表不是 `Win32_Process` 的天然字段。需要实测 `netstat`、`GetExtendedTcpTable` helper 和远程命令包装的 P50/P95，再决定实现。

### 8.4 不应移除 PsExec 的真实价值场景

以下场景仍应保留 PsExec：

- 脚本和长命令；
- 实时输出；
- 交互桌面/GUI/MMC；
- WMI/DCOM 被策略阻断；
- SMB/服务/DCOM 组合中 WMI 无法覆盖的执行形态。

### 8.5 不应破坏破坏性操作的重放保护

服务停止、删除文件、卸载软件、删除注册表、终止进程等操作，一旦远端已实际启动，返回非零必须视为命令失败，不能通过另一通道再次执行。

---

## 9. 分阶段实施建议

### 阶段 0：建立基线，不改行为

- 收集能力探测冷/热耗时；
- 记录每种功能的实际通道和回退次数；
- 建立小命令、长脚本、服务、注册表、软件、磁盘、进程、端口用例；
- 记录 P50/P95、首字节时间和失败分类。

### 阶段 1：P0 优化

1. 能力探测按 operation/profile 分层。
2. 进程列表改为直接 WMI `Win32_Process` 优先。
3. 注册表值和环境变量读取消除 N+1 WMI 调用。
4. 服务属性窗口复用 session/scope，减少串行查询。
5. 清理磁盘、进程、端口、注册表的重复探测和无意义回退。
6. 保持现有 transport failure 规则不变。

### 阶段 2：P1 优化

1. WMI scope/连接复用和受控并发。
2. 服务状态轮询替代固定等待。
3. 磁盘/会话/日志刷新降频和去重。
4. 打印机、设备驱动匹配和软件批量枚举优化。
5. UI 增加明确的“缓存命中/正在查询/正在回退”状态，但不要让进度条成为唯一反馈。

### 阶段 3：P2 优化

1. 路由学习增加延迟分位和命令形态。
2. 统一基准测试和性能回归门槛。
3. 评估远程原生 helper（注册表、连接表、打印机）的收益与部署风险。
4. 形成按功能可配置的路由策略，而不是全局固定偏好。

---

## 10. 验证建议

| 测试场景 | 验证重点 |
|---|---|
| WMI 可用、PsExec 可用 | 只读查询不得启动 PsExec；普通短命令按策略选择 |
| WMI 可用、PsExec 不可用 | 查询和短命令正常，结果一致 |
| WMI 不可用、PsExec 可用 | 只在 transport failure 后回退 PsExec |
| 远端已启动但返回非零 | 不得换通道重放，UI 应显示原始退出码和错误 |
| 长命令超过 700 字符 | 应走文件化或 WMI raw，不得因 RunAs 命令行失败 |
| 拖拽脚本 | SMB 上传、PsExec 流式输出、进度、退出码、取消都一致 |
| GUI/MMC 启停 | PsExec `-i` 后能进入正确桌面会话，回退不会重复启动 |
| 服务账户修改 | 域账户、UPN、`.\user`、特殊密码、服务不存在 1060、WMI 返回 5/22 |
| 服务重启 | 状态轮询，不出现固定 1500 ms 导致的提前 start |
| 注册表大键 | 值数量和往返次数、读取耗时、缓存命中一致 |
| 软件清单 | 64/32 位键、延迟加载、深度清理失效正确 |
| 进程/端口查询 | 数据准确、输出解析一致、无重复全量查询 |
| 本机目标 | 不出现 PsExec 自连接，不因本机 445/135/5985 关闭而误判 |
| 日志 | 无明文密码、无空行噪声、回退摘要清晰 |

每个用例至少记录：

- 目标主机和认证方式（不记录密码）；
- 当前版本/提交；
- 实际通道；
- 是否回退；
- 开始时间、首字节时间、完成时间；
- 退出码和错误分类；
- 远端结果与预期是否一致。

---

## 11. 代码证据索引

以下路径用于后续实施时定位，不表示本次修改过这些文件：

- `Architecture.md`：路由、能力快照、PsExec/RunAs 安全约束。
- `src/RemoteOpsTool/Services/Capability/CapabilityMatrix.cs`：操作到通道的顺序矩阵。
- `src/RemoteOpsTool/Services/Capability/CapabilityService.cs`：5 分钟快照缓存和凭据指纹。
- `src/RemoteOpsTool/Services/Transports/TransportProbeService.cs`：Ping、端口、ADMIN$、WMI、query user、PsExec、schtasks 全量探测。
- `src/RemoteOpsTool/Services/Transports/RemoteExecutionService.cs`：回退链执行、700 字符规则、transport failure 判定、命令非零禁止重放。
- `src/RemoteOpsTool/ViewModels/TerminalViewModel.cs`：普通终端命令默认路由、脚本 `PreferPsExec`、交互入口。
- `src/RemoteOpsTool/Services/PsExecService.cs`：PsExec RunAs、WMI raw registry job、轮询和超时。
- `src/RemoteOpsTool/Services/NetworkService.cs`：进程、连接、会话、网络命令。
- `src/RemoteOpsTool/Services/FileDiskService.cs`：磁盘查询、清理和缓存。
- `src/RemoteOpsTool/Services/ServiceManagerService.cs`、`src/RemoteOpsTool/ViewModels/Dialogs/ServicePropertiesViewModel.cs`：服务查询、配置和状态操作。
- `src/RemoteOpsTool/ViewModels/Dialogs/RemoteRegistryViewModel.cs`：注册表 WMI/命令回退和逐值读取。
- `src/RemoteOpsTool/Services/EnvVarService.cs`、`src/RemoteOpsTool/Services/SoftwareService.cs`、`src/RemoteOpsTool/Services/DeviceService.cs`、`src/RemoteOpsTool/Services/PrinterService.cs`、`src/RemoteOpsTool/Services/SystemInfoService.cs`：各功能模块的 WMI/命令回退。

---

## 12. 最终建议

如果按投入产出排序，建议先做：

1. **能力探测 profile 化**；
2. **进程列表直接 WMI `Win32_Process` 优先**；
3. **注册表/环境变量批量读取**；
4. **服务属性窗口 session/scope 复用和状态轮询**；
5. **磁盘、日志、会话刷新降频**；
6. **以基准数据决定端口、脚本和路由学习的后续优化**。

这六项不需要改变所有功能的用户语义，也不会破坏现有 PsExec 安全约束，但能显著减少“用户点开功能后先等待完整探测/命令启动/逐值往返”的体感延迟。

---

## 13. 实施状态与验证入口

本轮优化已在 `codex/优化架构` 分支实施，按报告项目映射如下：

| 报告项目 | 实施状态 | 主要落点 |
|---|---|---|
| P0-1 能力探测按 operation/profile 分层 | 已完成 | `CapabilityProbeProfile`、`CapabilityProbeCatalog`、`CapabilityService` |
| P0-2 进程列表优先直接 WMI | 已完成 | `NetworkService` |
| P0-3 活动连接优先原生 netstat | 已完成 | `NetworkService` 的 netstat 与并行 WMI 进程名映射 |
| P0-4 注册表值批量读取 | 已完成 | `RemoteRegistryBatchReader`、`RemoteRegistryViewModel`、`EnvVarService` |
| P0-5 服务属性复用 session/scope | 已完成 | `ServiceManagerService`、`ServicePropertiesViewModel` |
| P0-6 能力缓存和路由学习按 transport 隔离 | 已完成 | `CapabilitySnapshot`、`RouteLearningStore` |
| P1-1 WMI 连接复用和受控并发 | 已完成 | `RemoteWmiConnectionPool`、`RemoteWmiHelper` |
| P1-2 系统信息和磁盘选择性查询 | 已完成 | `SystemInfoService`、`FileDiskService` |
| P1-3 磁盘刷新降频和日志降噪 | 已完成 | `FileDiskService`、磁盘状态 ViewModel |
| P1-4 服务状态轮询 | 已完成 | `ServiceManagerService`、`ServicePropertiesViewModel` |
| P1-5 清默认打印机移除动态 Add-Type | 已完成 | `PrinterService` 改用固定 `WScript.Network` COM 调用 |
| P1-6 设备驱动匹配索引化 | 已完成 | `DeviceService` |
| P1-7 软件清单批量枚举和凭据隔离缓存 | 已完成 | `SoftwareService`、`CacheKeys` |
| P1-8 日志和输出通道去重 | 已完成 | `RemoteExecutionService` 回退输出去重、协议行折叠 |
| P2-1 路由学习延迟和回退指标 | 已完成 | `RouteLearningStore`、`CapabilityOutcome` |
| P2-2 统一基准测试 | 已完成文档和报表入口 | `tools/Report-RouteLearning.ps1`、`性能基准测试.md` |

验证入口：

```powershell
dotnet build RemoteOpsTool.slnx --no-restore
dotnet test RemoteOpsTool.slnx --no-restore
pwsh -NoProfile -File .\tools\Report-RouteLearning.ps1
```

注意：真实性能基准需要至少一台 WMI 可用目标、一台仅 PsExec 可用目标和一台本机目标；在没有真实多主机结果前，不把单元测试通过等同于生产性能达标。

---

**报告状态：评估完成；首轮全面优化已完成并留有可执行基准测试方案。**
